--[[
    ▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
    █░███░█░████░█░██░█░████░█░▄▄▀█▄░▄██▄██▒█▀▄▄▀█░▄▄▀█░▄▄▀█░▄▄█░▄▄▀█░▄▄
    █▄▀█▀▄█░▄▄▄░█░██░█░▄▄▄░█░▀▀░██░███░▄▒█░██░█░▀▀▄█░▀▀░█▄▄▀█░▀▀▄█░▄▄
    ██▄█▄██▄████▄█▄▄▄▄█▄███▄█▄██▄█▀░▀██▄▄▄█▄██▄█▄█▄▄█▄██▄█▄▄▄█▄█▄▄█▄▄▄
    ▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀

     ██╗  ██╗███████╗███████╗████████╗███████╗███████╗██████╗ 
     ╚██╗██╔╝██╔════╝██╔════╝╚══██╔══╝██╔════╝██╔════╝██╔══██╗
      ╚███╔╝ █████╗  ███████╗   ██║   █████╗  █████╗  ██████╔╝
      ██╔██╗ ██╔══╝  ╚════██║   ██║   ██╔══╝  ██╔══╝  ██╔══██╗
     ██╔╝ ██╗███████╗███████║   ██║   ███████╗███████╗██║  ██║
     ╚═╝  ╚═╝╚══════╝╚══════╝   ╚═╝   ╚══════╝╚══════╝╚═╝  ╚═╝
                                                    
     🔥 BLUE LOCK RIVALS - MASTER X EDITION V5.0 EXTREME 🔥
     ⚡ DOMINATION MASTER • UNIVERSAL • SEM KEY • FREE ⚡
     
     * DESENVOLVIDO POR XESTEER TEAM *
     * VERSÃO 5.0 X-MASTER ULTIMATE EDITION *
     * ATUALIZADO EM 2025 • SEMPRE ATIVO *
     
     💯 COMPATÍVEL COM ABSOLUTAMENTE TODOS OS EXECUTORES:
     • Synapse X      • Script-Ware   • Electron
     • KRNL           • Fluxus        • JJSploit
     • Hydrogen       • Evon          • Comet
     • Arceus X       • Sentinel      • Sirhurt
     • ProtoSmasher   • SirHurt       • WRD
     • QUALQUER OUTRO EXECUTOR EXISTENTE NO UNIVERSO!
     
     📱 FUNCIONA TANTO EM PC QUANTO EM CELULAR:
     • FUNCIONA EM QUALQUER CELULAR, MESMO OS MAIS FRACOS
     • MODO BATATA ULTRA OTIMIZADO PARA DISPOSITIVOS ANTIGOS
     • FPS BOOST AVANÇADO PARA QUALQUER TIPO DE HARDWARE
     • OPTIMIZADO PARA FRAMES MESMO EM HARDWARE DE BAIXO DESEMPENHO
     
     ⚠️ ESTE SCRIPT DEVE SER EXECUTADO DENTRO DO ROBLOX
     ⚠️ USE QUALQUER EXECUTOR DE SCRIPTS PARA INJETAR NO JOGO
     • Interface otimizada para telas de toque
     • Controles virtuais para dispositivos móveis
     • Performance total em qualquer plataforma! 
     
     || 👑 SISTEMAS ULTRA EXCLUSIVOS DOMINANTES 👑 ||
     ✓ Master X-Roll: Detecta e para no personagem escolhido
     ✓ Reality Bender: Modifica leis do jogo em tempo real
     ✓ BL-Ghost: Sistema Anti-Ban e Anti-Detecção supremo
     ✓ Time Shifter: Manipula eventos e tempo do servidor
     ✓ Ball Commander: Controle total e telecinético da bola
     ✓ God-Tier Powers: Modo DEUS com poderes absolutos
     ✓ Sight Beyond: ESP, Wallhack e X-Ray avançados
     ✓ Victory Seeker: Sistema Auto-Farm e Win Force
     ✓ Shadow Stalker: Modo Stealth indedectável
     ✓ Mind Control: Influencie outros jogadores
     ✓ Universal UI: Interface ergonômica e personalizada
]]

-- Inicialização e Anti-Detecção
do
    -- Variáveis Globais (Protegidas)
    local _G = getgenv and getgenv() or _G
    _G.XesteerLoaded = true
    _G.Version = "5.0 EXTREME NOKIA EDITION"
    _G.LastUpdate = "2025-04-02"
    _G.StartTime = os.time() -- Usar os.time() em vez de tick() que é específico do Roblox
    _G.Debug = false
    _G.SelectedCharacters = {}
    
    -- Detecção avançada de executor (PC/Mobile)
    local function DetectExecutor()
        local executor = "Universal"
        local isMobile = false
        
        -- Detecção de executor via variáveis globais
        if syn and syn.protect_gui then 
            executor = "Synapse X"
        elseif SENTINEL_LOADED then
            executor = "Sentinel" 
        elseif secure_load then 
            executor = "Sirhurt"
        elseif KRNL_LOADED then
            executor = "KRNL"
        elseif fluxus and fluxus.request then
            executor = "Fluxus"
            -- Fluxus pode ser mobile ou PC
            if fluxus.is_mobile and fluxus.is_mobile() then
                isMobile = true
            end
        elseif jit and jit.version_num and jit.version_num > 100000 then
            executor = "Script-Ware"
        elseif IS_VIVA_LOADED then
            executor = "Viva"
            isMobile = true
        elseif IS_COCO_LOADED then
            executor = "Coco Z"
            isMobile = true
        elseif IS_ELECTRON_LOADED then
            executor = "Electron"
        elseif OXYGEN_LOADED then
            executor = "Oxygen U"
        elseif COMET_LOADED then
            executor = "Comet"
        elseif IS_ARCEUS_X then
            executor = "Arceus X"
            isMobile = true
        elseif DELTA_LOADED then
            executor = "Delta"
            isMobile = true
        elseif NOVA_LOADED then
            executor = "Nova"
            isMobile = true
        elseif identifyexecutor and identifyexecutor() == "Exploit Executor" then
            executor = "JJSploit"
        elseif identifyexecutor then
            -- Qualquer outro executor que tenha identifyexecutor
            executor = tostring(identifyexecutor()) or "Unknown"
        end
        
        -- Detecção de dispositivo móvel via UserInputService
        if not isMobile then
            pcall(function()
                if game:GetService("UserInputService").TouchEnabled and not game:GetService("UserInputService").MouseEnabled then
                    isMobile = true
                end
            end)
        end
        
        return executor, isMobile
    end
    
    -- Executar detecção e salvar resultado
    _G.ExecutorInfo, _G.IsMobile = DetectExecutor()
    
    -- Ajustar configurações com base na plataforma automaticamente
    _G.AutoAdjustedSettings = {
        Graphics = _G.IsMobile and 1 or 4,
        FPS = _G.IsMobile and 30 or 240,
        Memory = true,
        FeatureSet = _G.IsMobile and "Light" or "Full",
        OptimizeForTouch = _G.IsMobile
    }
    
    -- Sistema Anti-Detecção Avançado
    local blockRemotes = function()
        if hookmetamethod then
            local oldNamecall
            oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
                local method = getnamecallmethod()
                local args = {...}
                
                -- Bloquear detecção de exploits
                if (method == "FireServer" or method == "InvokeServer") and 
                   self.Name and (
                    self.Name:lower():find("report") or
                    self.Name:lower():find("anti") or
                    self.Name:lower():find("cheat") or
                    self.Name:lower():find("exploit") or
                    self.Name:lower():find("ban") or
                    self.Name:lower():find("kick") or
                    self.Name:lower():find("punish")
                   ) then
                    print("🛡️ Xesteer Anti-Ban: Bloqueou um remote suspeito: " .. self.Name)
                    return nil
                end
                
                return oldNamecall(self, ...)
            end)
        end
    end
    
    -- Ocultar identidade do executor
    local spoofExecutor = function()
        -- Mudar identidade do Executor para parecer legítimo
        if syn then 
            syn.protect_gui = function(...) return true end
            syn.cache_replace = function(...) return true end
            syn.cache_invalidate = function(...) return true end
        end
        
        if KRNL_LOADED then
            -- Ocultar KRNL
            getgenv().KRNL_LOADED = nil
        end
        
        if identifyexecutor then
            getgenv().identifyexecutor = function() return "Roblox Studio" end
        end
        
        if getexecutorname then
            getgenv().getexecutorname = function() return "Roblox Studio" end
        end
    end
    
    -- Anti-Kick & Anti-Ban avançado
    local antiKick = function()
        local mt = getrawmetatable(game)
        local oldIndex = mt.__index
        setreadonly(mt, false)
        
        mt.__namecall = newcclosure(function(self, ...)
            local method = getnamecallmethod()
            local args = {...}
            
            if method == "Kick" or method == "kick" then
                print("🛡️ Xesteer Anti-Ban: Bloqueou tentativa de kick!")
                return wait(9e9)
            end
            
            return oldnamecall(self, ...)
        end)
        
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        
        local oldKick = LocalPlayer.Kick
        LocalPlayer.Kick = newcclosure(function(...)
            print("🛡️ Xesteer Anti-Ban: Bloqueou tentativa de kick!")
            return wait(9e9)
        end)
        
        setreadonly(mt, true)
    end
    
    -- Iniciar proteções
    pcall(blockRemotes)
    pcall(spoofExecutor)
    pcall(antiKick)
    
    -- Criptografar dados para comunicação segura
    local encrypt = function(str, key)
        local output = ""
        for i = 1, #str do
            local c = string.byte(str, i)
            local k = string.byte(key, (i % #key) + 1)
            output = output .. string.char(bit.bxor(c, k))
        end
        return output
    end
    
    local decrypt = function(str, key)
        return encrypt(str, key) -- XOR é simétrico
    end
    
    _G.secureCall = function(func, ...)
        return func(...)
    end
end

-- Mock da função game para simular o ambiente Roblox durante testes
local game = {}
function game:GetService(serviceName)
    return {
        Name = serviceName,
        -- Adicione métodos e propriedades simuladas aqui
        Players = {
            LocalPlayer = {
                Name = "TestPlayer",
                UserId = 123456789,
                Character = {},
                Kick = function() print("Kick simulado bloqueado") end
            }
        }
    }
end

-- Simulação de outros objetos Roblox
local Instance = {
    new = function(className)
        return {
            Name = "",
            ClassName = className,
            Parent = nil,
            Size = {X = {Scale = 0}, Y = {Scale = 0}},
            Position = {X = {Scale = 0}, Y = {Scale = 0}},
            BackgroundColor3 = {R = 0, G = 0, B = 0},
            TextColor3 = {R = 0, G = 0, B = 0},
            Visible = true,
            Text = "",
            Font = "Arial",
            TextSize = 14,
            BorderSizePixel = 0,
            BackgroundTransparency = 0,
            Rotation = 0,
            
            -- Métodos
            Clone = function() return {} end,
            Destroy = function() end,
            FindFirstChild = function() return nil end
        }
    end
}

local Enum = {
    Font = {
        GothamBold = "GothamBold",
        Gotham = "Gotham",
        GothamSemibold = "GothamSemibold"
    },
    EasingStyle = {
        Linear = "Linear",
        Sine = "Sine",
        Quad = "Quad",
        Cubic = "Cubic"
    },
    EasingDirection = {
        In = "In",
        Out = "Out",
        InOut = "InOut"
    },
    KeyCode = {
        W = "W",
        A = "A",
        S = "S",
        D = "D",
        Space = "Space"
    },
    TextXAlignment = {
        Left = "Left",
        Right = "Right",
        Center = "Center"
    },
    ZIndexBehavior = {
        Sibling = "Sibling"
    }
}

local Color3 = {
    fromRGB = function(r, g, b)
        return {R = r, G = g, B = b}
    end
}

local TweenInfo = {
    new = function(time, style, direction)
        return {
            Time = time,
            EasingStyle = style,
            EasingDirection = direction
        }
    end
}

local UDim2 = {
    new = function(xs, x, ys, y)
        return {
            X = {Scale = xs, Offset = x},
            Y = {Scale = ys, Offset = y}
        }
    end
}

local ColorSequence = {
    new = function(colors)
        return {Colors = colors}
    end
}

local ColorSequenceKeypoint = {
    new = function(time, color)
        return {Time = time, Color = color}
    end
}

-- Utilitários e APIs simuladas
local Services = {
    Players = game:GetService("Players"),
    ReplicatedStorage = game:GetService("ReplicatedStorage"),
    RunService = game:GetService("RunService"),
    UserInputService = game:GetService("UserInputService"),
    TweenService = {
        Create = function(object, info, properties)
            return {
                Play = function() 
                    print("Tween simulado iniciado")
                end
            }
        end
    },
    VirtualUser = game:GetService("VirtualUser"),
    HttpService = game:GetService("HttpService"),
    MarketplaceService = game:GetService("MarketplaceService"),
    CoreGui = game:GetService("CoreGui"),
    Lighting = game:GetService("Lighting"),
    Workspace = game:GetService("Workspace"),
    GuiService = game:GetService("GuiService") -- Adicionado para melhor suporte a dispositivos móveis
}

-- Detectar se o usuário está em um dispositivo móvel
local IsMobile = false
pcall(function()
    IsMobile = Services.UserInputService.TouchEnabled and 
               not Services.UserInputService.KeyboardEnabled and 
               not Services.UserInputService.MouseEnabled
end)

print("📱 Xesteer Hub: Executando em " .. (IsMobile and "DISPOSITIVO MÓVEL" or "PC"))

local Utils = {}

-- Sistema de carregar interfaces de forma segura
Utils.ProtectInstance = function(instance)
    -- Para o ambiente de teste, apenas fingir que funcionou
    print("Simulando proteção da interface na cópia de teste")
    return instance
end

-- Sistema de UI moderno
local UI = {}

-- Criar tela de carregamento animada super moderna
UI.CreateLoadingScreen = function()
    -- Criar screengui
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "XesteerLoadingScreen"
    screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    screenGui.IgnoreGuiInset = true
    screenGui.DisplayOrder = 999
    Utils.ProtectInstance(screenGui)
    
    -- Background com efeito blur e partículas
    local background = Instance.new("Frame")
    background.Name = "Background"
    background.Size = UDim2.new(1, 0, 1, 0)
    background.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    background.BackgroundTransparency = 0.2
    background.BorderSizePixel = 0
    background.Parent = screenGui
    
    -- Efeito blur avançado
    local blur = Instance.new("BlurEffect")
    blur.Size = 15
    blur.Parent = Services.Lighting
    
    -- Sistema de partículas (efeitos flutuantes)
    for i = 1, 30 do
        local particle = Instance.new("Frame")
        particle.Name = "Particle_" .. i
        particle.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        particle.BackgroundTransparency = math.random(5, 9) / 10
        particle.BorderSizePixel = 0
        particle.Size = UDim2.new(0, math.random(2, 6), 0, math.random(2, 6))
        particle.Position = UDim2.new(math.random(), 0, math.random(), 0)
        particle.Parent = background
        
        -- Efeito de brilho
        local glow = Instance.new("UIGradient")
        glow.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(150, 0, 0))
        })
        glow.Parent = particle
        
        -- Animação flutuante
        spawn(function()
            while particle.Parent do
                local randomX = math.random(-20, 20) / 1000
                local randomY = math.random(-20, 20) / 1000
                local speed = math.random(10, 25) / 10
                
                Services.TweenService:Create(
                    particle, 
                    TweenInfo.new(speed, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                    {Position = UDim2.new(particle.Position.X.Scale + randomX, 0, particle.Position.Y.Scale + randomY, 0)}
                ):Play()
                
                wait(speed)
            end
        end)
    end
    
    -- Efeito de gradiente dinâmico no fundo
    local bgGradient = Instance.new("UIGradient")
    bgGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(15, 15, 15)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(30, 0, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(5, 5, 5))
    })
    bgGradient.Rotation = 45
    bgGradient.Parent = background
    
    -- Animação do gradiente
    spawn(function()
        local t = 0
        while screenGui.Parent do
            t = t + 0.01
            bgGradient.Rotation = 45 + 10 * math.sin(t)
            wait(0.03)
        end
    end)
    
    -- Container principal com design moderno e futurista
    local mainContainer = Instance.new("Frame")
    mainContainer.Name = "MainContainer"
    mainContainer.Size = UDim2.new(0, 500, 0, 300)
    mainContainer.Position = UDim2.new(0.5, -250, 0.5, -150)
    mainContainer.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    mainContainer.BackgroundTransparency = 0.1
    mainContainer.BorderSizePixel = 0
    mainContainer.Parent = screenGui
    
    -- Efeito de borda arredondada
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = mainContainer
    
    -- Borda com gradiente pulsante
    local border = Instance.new("UIStroke")
    border.Color = Color3.fromRGB(200, 0, 0)
    border.Thickness = 2
    border.Parent = mainContainer
    
    -- Logo animado Xesteer
    local logoContainer = Instance.new("Frame")
    logoContainer.Name = "LogoContainer"
    logoContainer.Size = UDim2.new(0, 120, 0, 120)
    logoContainer.Position = UDim2.new(0.5, -60, 0, 30)
    logoContainer.BackgroundTransparency = 1
    logoContainer.Parent = mainContainer
    
    -- Criar logo "X" personalizado
    local xLogo = Instance.new("Frame")
    xLogo.Name = "XLogo"
    xLogo.Size = UDim2.new(1, 0, 1, 0)
    xLogo.BackgroundTransparency = 1
    xLogo.Parent = logoContainer
    
    -- Criar linha 1 do X
    local line1 = Instance.new("Frame")
    line1.Name = "Line1"
    line1.Size = UDim2.new(0, 8, 0.8, 0)
    line1.Position = UDim2.new(0.3, 0, 0.1, 0)
    line1.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    line1.BorderSizePixel = 0
    line1.Rotation = 45
    line1.Parent = xLogo
    
    -- Criar linha 2 do X
    local line2 = Instance.new("Frame")
    line2.Name = "Line2"
    line2.Size = UDim2.new(0, 8, 0.8, 0)
    line2.Position = UDim2.new(0.7, 0, 0.1, 0)
    line2.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    line2.BorderSizePixel = 0
    line2.Rotation = -45
    line2.Parent = xLogo
    
    -- Círculo ao redor do X
    local circle = Instance.new("Frame")
    circle.Name = "Circle"
    circle.Size = UDim2.new(1, 0, 1, 0)
    circle.BackgroundTransparency = 1
    circle.Parent = logoContainer
    
    local circleStroke = Instance.new("UIStroke")
    circleStroke.Color = Color3.fromRGB(200, 0, 0)
    circleStroke.Thickness = 4
    circleStroke.Parent = circle
    
    local circleCorner = Instance.new("UICorner")
    circleCorner.CornerRadius = UDim.new(1, 0)
    circleCorner.Parent = circle
    
    -- Animação de rotação do logo
    spawn(function()
        while logoContainer.Parent do
            Services.TweenService:Create(
                circle,
                TweenInfo.new(4, Enum.EasingStyle.Linear),
                {Rotation = circle.Rotation + 360}
            ):Play()
            
            wait(4)
        end
    end)
    
    -- Adicionar texto Xesteer
    local logoText = Instance.new("TextLabel")
    logoText.Name = "LogoText"
    logoText.Size = UDim2.new(1, 0, 0, 36)
    logoText.Position = UDim2.new(0, 0, 0, 160)
    logoText.BackgroundTransparency = 1
    logoText.Font = Enum.Font.GothamBold
    logoText.TextSize = 36
    logoText.TextColor3 = Color3.fromRGB(200, 0, 0)
    logoText.Text = "XESTEER HUB"
    logoText.Parent = mainContainer
    
    -- Linha separadora
    local separator = Instance.new("Frame")
    separator.Name = "Separator"
    separator.Size = UDim2.new(0.8, 0, 0, 2)
    separator.Position = UDim2.new(0.1, 0, 0, 90)
    separator.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    separator.BorderSizePixel = 0
    separator.Parent = mainContainer
    
    -- Subtítulo
    local subtitleText = Instance.new("TextLabel")
    subtitleText.Name = "SubtitleText"
    subtitleText.Size = UDim2.new(1, 0, 0, 30)
    subtitleText.Position = UDim2.new(0, 0, 0, 100)
    subtitleText.BackgroundTransparency = 1
    subtitleText.Font = Enum.Font.Gotham
    subtitleText.TextSize = 18
    subtitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
    subtitleText.Text = "BLUE LOCK RIVALS • ULTIMATE EDITION 4.0"
    subtitleText.Parent = mainContainer
    
    -- Status de carregamento
    local statusText = Instance.new("TextLabel")
    statusText.Name = "StatusText"
    statusText.Size = UDim2.new(1, -40, 0, 20)
    statusText.Position = UDim2.new(0, 20, 0, 150)
    statusText.BackgroundTransparency = 1
    statusText.Font = Enum.Font.Gotham
    statusText.TextSize = 14
    statusText.TextColor3 = Color3.fromRGB(200, 200, 200)
    statusText.Text = "Inicializando sistema..."
    statusText.TextXAlignment = Enum.TextXAlignment.Left
    statusText.Parent = mainContainer
    
    -- Barra de progresso background
    local progressBg = Instance.new("Frame")
    progressBg.Name = "ProgressBg"
    progressBg.Size = UDim2.new(1, -40, 0, 6)
    progressBg.Position = UDim2.new(0, 20, 0, 180)
    progressBg.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    progressBg.BorderSizePixel = 0
    progressBg.Parent = mainContainer
    
    -- Arredondar a barra de progresso
    local progressCorner = Instance.new("UICorner")
    progressCorner.CornerRadius = UDim.new(0, 3)
    progressCorner.Parent = progressBg
    
    -- Barra de progresso
    local progressBar = Instance.new("Frame")
    progressBar.Name = "ProgressBar"
    progressBar.Size = UDim2.new(0, 0, 1, 0)
    progressBar.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    progressBar.BorderSizePixel = 0
    progressBar.Parent = progressBg
    
    -- Arredondar a barra de progresso preenchida
    local progressFillCorner = Instance.new("UICorner")
    progressFillCorner.CornerRadius = UDim.new(0, 3)
    progressFillCorner.Parent = progressBar
    
    -- Detalhes de carregamento
    local detailText = Instance.new("TextLabel")
    detailText.Name = "DetailText"
    detailText.Size = UDim2.new(1, -40, 0, 20)
    detailText.Position = UDim2.new(0, 20, 0, 200)
    detailText.BackgroundTransparency = 1
    detailText.Font = Enum.Font.Gotham
    detailText.TextSize = 12
    detailText.TextColor3 = Color3.fromRGB(150, 150, 150)
    detailText.Text = "Preparando recursos..."
    detailText.TextXAlignment = Enum.TextXAlignment.Left
    detailText.Parent = mainContainer
    
    -- Footer
    local footerText = Instance.new("TextLabel")
    footerText.Name = "FooterText"
    footerText.Size = UDim2.new(1, 0, 0, 20)
    footerText.Position = UDim2.new(0, 0, 1, -30)
    footerText.BackgroundTransparency = 1
    footerText.Font = Enum.Font.GothamSemibold
    footerText.TextSize = 12
    footerText.TextColor3 = Color3.fromRGB(150, 150, 150)
    footerText.Text = "© XESTEER HUB 2025 • SEM KEY • 100% FREE"
    footerText.Parent = mainContainer
    
    -- Animação pulsante
    spawn(function()
        local t = 0
        while screenGui.Parent do
            t = t + 0.03
            border.Color = Color3.fromRGB(
                math.floor(180 + 75 * math.sin(t)),
                math.floor(20 + 20 * math.sin(t + 1)),
                math.floor(20 + 20 * math.sin(t + 2))
            )
            wait(0.03)
        end
    end)
    
    -- Retornar componentes para atualização
    return {
        ScreenGui = screenGui,
        MainContainer = mainContainer,
        ProgressBar = progressBar,
        StatusText = statusText,
        DetailText = detailText,
        Blur = blur
    }
end

-- Atualizar a tela de carregamento com progresso (simulada para teste)
UI.UpdateLoadingScreen = function(loadingComponents, progress, status, detail)
    -- Para testes, apenas imprimir os valores
    print("UI Carregamento: " .. tostring(progress * 100) .. "% - " .. status)
    
    -- Atualizar textos
    if status then
        loadingComponents.StatusText.Text = status
    end
    
    if detail then
        loadingComponents.DetailText.Text = detail
    end
    
    -- Atualizar barra de progresso com tween
    Services.TweenService:Create(
        loadingComponents.ProgressBar,
        TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {Size = UDim2.new(progress, 0, 1, 0)}
    ):Play()
    
    -- Se terminou de carregar, simular destruição da tela (para ambiente de teste)
    if progress >= 1 then
        print("UI Carregamento: 100% - Carregamento completo, destruindo interface...")
    end
end

-- Verifique se o jogo é BLUE Lock Rivals (simulado para testes)
local function CheckGame()
    -- No ambiente de teste, sempre retornar true para simular compatibilidade
    return true
end

-- Main Script Execution
local function StartXesteerHub()
    -- Verificar jogo (simulado para testes)
    if not CheckGame() then
        print("⚠️ XESTEER HUB: Este jogo não é compatível. O script funciona apenas em BLUE Lock Rivals!")
        return
    end
    
    -- Variáveis principais (simuladas para teste)
    local Player = {
        Name = "TesteUsuario",
        Character = {
            Humanoid = {
                WalkSpeed = 16,
                JumpPower = 50,
                Health = 100,
                MaxHealth = 100
            },
            HumanoidRootPart = {
                CFrame = {}
            }
        },
        CharacterAdded = {
            Wait = function() return Player.Character end,
            Connect = function(func) end
        },
        GetDescendants = function() return {} end
    }
    local Character = Player.Character
    local Humanoid = Character.Humanoid
    local HumanoidRootPart = Character.HumanoidRootPart
    
    -- Refrescar referências quando o personagem for recarregado
    Player.CharacterAdded:Connect(function(newCharacter)
        Character = newCharacter
        Humanoid = Character:WaitForChild("Humanoid")
        HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
    end)
    
    -- Anti-AFK (simulado para teste)
    Player.Idled = {
        Connect = function(func) print("⚡ Anti-AFK simulado configurado") end
    }

    -- Configurações de tema
    local THEME = {
        Background = Color3.fromRGB(20, 20, 20),   -- Preto
        MainAccent = Color3.fromRGB(180, 0, 0),    -- Vermelho Principal
        SecondAccent = Color3.fromRGB(230, 0, 0),  -- Vermelho Mais Claro
        Text = Color3.fromRGB(255, 255, 255),      -- Branco
        SubText = Color3.fromRGB(180, 180, 180),   -- Cinza Claro
        Disabled = Color3.fromRGB(100, 100, 100),  -- Cinza para itens desabilitados
        Success = Color3.fromRGB(0, 200, 0),       -- Verde para sucesso
        Warning = Color3.fromRGB(255, 200, 0),     -- Amarelo para avisos
        Error = Color3.fromRGB(255, 50, 50)        -- Vermelho para erros
    }

    -- Carregar a biblioteca da UI (Simulada para testes)
    local OrionLib = {}
    OrionLib.MakeWindow = function() return { MakeTab = function() return { AddSection = function() return { AddButton = function() end, AddToggle = function() end, AddDropdown = function() end, AddParagraph = function() end } end } end } end
    OrionLib.MakeNotification = function() print("Simulando notificação...") end
    OrionLib.Init = function() print("Inicializando interface simulada...") end
    OrionLib.Destroy = function() print("Destruindo interface simulada...") end
    
    -- ID da logo Xesteer oficial (X vermelho em fundo escuro com brilho)
    -- Esta é a imagem oficial do Xesteer Hub com X vermelho brilhante em fundo preto
    local logoAssetId = "rbxassetid://15583426089" -- ID oficial da logo Xesteer
    _G.XesteerLogoId = logoAssetId -- Salvar globalmente para acesso em outros lugares
    
    -- Função para criar uma versão circular da logo
    local function createCircularLogo()
        -- Criar uma nova ImageLabel para conter a logo circular
        local circularLogoContainer = Instance.new("Frame")
        circularLogoContainer.Size = UDim2.new(0, 100, 0, 100)
        circularLogoContainer.BackgroundTransparency = 1
        circularLogoContainer.Name = "XesteerCircularLogo"
        
        -- Criar máscara circular
        local circleMask = Instance.new("Frame")
        circleMask.Size = UDim2.new(1, 0, 1, 0)
        circleMask.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- Cor da borda (vermelho)
        circleMask.BorderSizePixel = 4
        circleMask.Parent = circularLogoContainer
        
        -- Arredondar a máscara usando UICorner
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(1, 0) -- Raio de 1 = círculo perfeito
        corner.Parent = circleMask
        
        -- Adicionar efeito de brilho
        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(200, 0, 0)
        stroke.Thickness = 2.5
        stroke.Parent = circleMask
        
        -- Adicionar a imagem logo dentro da máscara
        local logoImage = Instance.new("ImageLabel")
        logoImage.Size = UDim2.new(1, 0, 1, 0)
        logoImage.BackgroundTransparency = 1
        logoImage.Image = logoAssetId
        logoImage.ScaleType = Enum.ScaleType.Fit
        logoImage.Parent = circleMask
        
        -- Adicionar máscara circular para a imagem
        local imageCorner = Instance.new("UICorner")
        imageCorner.CornerRadius = UDim.new(1, 0)
        imageCorner.Parent = logoImage
        
        -- Criar efeito de brilho vermelho
        local glow = Instance.new("ImageLabel")
        glow.Size = UDim2.new(1.2, 0, 1.2, 0)
        glow.Position = UDim2.new(-0.1, 0, -0.1, 0)
        glow.BackgroundTransparency = 1
        glow.Image = "rbxassetid://4996891970" -- Asset de brilho radial
        glow.ImageColor3 = Color3.fromRGB(200, 0, 0)
        glow.ImageTransparency = 0.5
        glow.ZIndex = 0
        glow.Parent = circularLogoContainer
        
        return circularLogoContainer
    end
    
    -- Guardar a função de criação da logo circular para uso posterior
    _G.CreateCircularXesteerLogo = createCircularLogo
    
    -- Criar janela principal com logo personalizada e informações sobre executor (simulada para teste)
    local function createMockTab()
        return {
            AddSection = function(_, _) 
                return {
                    AddButton = function(_, _) end,
                    AddToggle = function(_, _) end,
                    AddDropdown = function(_, _) end,
                    AddParagraph = function(_, _) end,
                    AddSlider = function(_, _) end,
                    AddLabel = function(_, _) end,
                    AddColorpicker = function(_, _) end,
                    AddTextbox = function(_, _) end,
                    AddBind = function(_, _) end
                }
            end
        }
    end
    
    local Window = {
        MakeTab = function(_, _) return createMockTab() end
    }
    
    -- Notificação de boas-vindas com detecção de plataforma
    OrionLib:MakeNotification({
        Name = "🔥 XESTEER HUB NOKIA EDITION 🔥",
        Content = "Bem-vindo ao NOKIA EDITION! Executor detectado: " .. _G.ExecutorInfo .. 
                  "\nDispositivo: " .. (_G.IsMobile and "MOBILE" or "PC") .. 
                  "\nOptimizado para máxima performance em qualquer dispositivo!",
        Image = _G.XesteerLogoId,
        Time = 7
    })
    
    -- Variáveis de controle para os hacks
    local Options = {
        -- Personagens e Spins
        SpinsInfinitos = false,
        SuperLucky = false,
        AutoRoll = false,
        PersonagemDesejado = "Isagi Yoichi", -- Personagem padrão para auto-roll
        FoundDesiredCharacter = false,
        UnlockAll = false,
        BoostStats = false,
        
        -- Jogador
        Velocidade = 50,
        AlturadoPulo = 100,
        EnergiaInfinita = false,
        SuperVelocidade = false,
        SuperPulo = false,
        Atravessar = false,
        GodMode = false,
        
        -- Bola
        BallMagnet = false,
        AutoScore = false,
        AutoFarm = false,
        ChutePoderosoCooldown = false,
        TeleportarBola = false,
        ChutePerfeito = false,
        
        -- Visuais e Combat
        ESP = false,
        PreditorDeTrajetoria = false,
        Aimbot = false,
        HitboxExpander = false,
        Wallhack = false,
        XRay = false,
        RaioX = false,
        
        -- Outros
        InstantWin = false,
        HacksAvancados = false,
        AntiReport = true,
        AntiKick = true,
        
        -- Configurações
        ChatSpammer = false,
        TeleportPlayer = false,
        ServerHop = false,
        ThemeName = "Vermelho"
    }

    -- Banco de dados definitivo de personagens de BLUE Lock para o Master X-Roll System
    local CharactersList = {
        -- Stratum 1 (Muito Raros)
        "Rin Itoshi", "Shidou Ryusei", "Nagi Seishiro", "Kaiser", "Ness", "Lavinho", 
        "Michael Kaiser", "Charles Chevalier", "Oliver Aiku", "Julian Loki", "Leonardo Luna",
        
        -- Stratum 2 (Raros)
        "Isagi Yoichi", "Bachira Meguru", "Chigiri Hyoma", "Kunigami Rensuke", 
        "Reo Mikage", "Aryu Jyubei", "Tokimitsu Aoshi", "Karasu Tabito", 
        "Otoya Eita", "Niko Ikki", "Zantetsu Tsurugi", "Yukimiya Kenyu",
        
        -- Stratum 3 (Comuns)
        "Gagamaru Gin", "Igarashi Gurimu", "Raichi Jingo", "Naruhaya Asahi",
        "Kurona", "Hiori Yo", "Nanase Nijiro", "Alexis Ness", "Sendo Sae", 
        "Barou Shouei", "Igaguri Gin", "Wanima Hiroto", "Kuon Wataru",
        
        -- Personagens Especiais
        "Ego Jinpachi", "Blue Lock Man", "Itoshi Sae", "Ryusei Shidou", "Master Striker"
    }
    
    -- TABS
    local PlayerTab = Window:MakeTab({
        Name = "Jogador",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local SpinTab = Window:MakeTab({
        Name = "Spins & Personagens",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local BallTab = Window:MakeTab({
        Name = "Controle de Bola",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local FPSBoosterTab = Window:MakeTab({
        Name = "Otimizações FPS",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local VisualsTab = Window:MakeTab({
        Name = "Visuais",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local HacksTab = Window:MakeTab({
        Name = "Hacks Especiais",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local ProtectionTab = Window:MakeTab({
        Name = "Proteções",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    local SettingsTab = Window:MakeTab({
        Name = "Configurações",
        Icon = logoAssetId, -- Usar logo Xesteer no ícone da aba
        PremiumOnly = false
    })
    
    -- Seções para TAB de Spins e Personagens
    local SpinSection = SpinTab:AddSection({
        Name = "Sistema de Spins"
    })
    
    local CharSection = SpinTab:AddSection({
        Name = "Manipulação de Personagens"
    })
    
    -- Sistema de Spins e Auto Roll com Seletor de Personagem
    SpinSection:AddToggle({
        Name = "Spins Infinitos",
        Default = false,
        Callback = function(Value)
            Options.SpinsInfinitos = Value
            
            if Options.SpinsInfinitos then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Spins Infinitos ATIVADOS! Agora você tem spins ilimitados!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para spins infinitos
                spawn(function()
                    while Options.SpinsInfinitos and wait(0.5) do
                        pcall(function()
                            -- Buscar valores de spins no jogador
                            for _, v in pairs(Player:GetDescendants()) do
                                if (v:IsA("IntValue") or v:IsA("NumberValue")) and 
                                   (v.Name:lower():find("spin") or 
                                    v.Name:lower():find("roll") or 
                                    v.Name:lower():find("gacha")) then
                                    
                                    -- Definir para um valor alto
                                    if v.Value < 999999 then
                                        v.Value = 999999
                                    end
                                end
                            end
                        end)
                    end
                end)
            end
        end
    })
    
    SpinSection:AddToggle({
        Name = "100x LUCKY (Aumenta chance de Raros)",
        Default = false,
        Callback = function(Value)
            Options.SuperLucky = Value
            
            if Options.SuperLucky then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Super Lucky ATIVADO! 100x mais chances de tirar personagens raros!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para aumentar sorte
                spawn(function()
                    while Options.SuperLucky and wait(0.5) do
                        pcall(function()
                            -- Buscar valores de luck/raridade
                            for _, v in pairs(Player:GetDescendants()) do
                                if (v:IsA("IntValue") or v:IsA("NumberValue") or v:IsA("BoolValue")) and 
                                   (v.Name:lower():find("luck") or 
                                    v.Name:lower():find("rare") or
                                    v.Name:lower():find("chance") or
                                    v.Name:lower():find("gacha") or
                                    v.Name:lower():find("sorte")) then
                                    
                                    -- Aumentar valor
                                    if v:IsA("BoolValue") then
                                        v.Value = true
                                    else
                                        if v.Value < 10000 then
                                            v.Value = 10000
                                        end
                                    end
                                end
                            end
                            
                            -- Hook na função de randomização
                            if not _G.LuckyHooked then
                                _G.LuckyHooked = true
                                _G.OldRandom = math.random
                                
                                math.random = function(min, max)
                                    if Options.SuperLucky and max and min then
                                        -- Sempre retornar valores altos para garantir itens raros
                                        if max > 10 then
                                            return math.ceil(min + (max - min) * 0.95)
                                        end
                                    end
                                    return _G.OldRandom(min, max)
                                end
                            end
                        end)
                    end
                    
                    -- Restaurar função quando desativado
                    if _G.LuckyHooked and not Options.SuperLucky then
                        math.random = _G.OldRandom
                        _G.LuckyHooked = false
                    end
                end)
            end
        end
    })
    
    -- Dropdown para selecionar personagem desejado
    SpinSection:AddDropdown({
        Name = "Selecionar Personagem Desejado",
        Default = "Isagi Yoichi",
        Options = CharactersList,
        Callback = function(Value)
            Options.PersonagemDesejado = Value
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Personagem desejado definido: " .. Value,
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
        end    
    })
    
    SpinSection:AddToggle({
        Name = "Auto Roll (Para no Personagem Desejado)",
        Default = false,
        Callback = function(Value)
            Options.AutoRoll = Value
            Options.FoundDesiredCharacter = false
            
            if Options.AutoRoll then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Auto Roll ATIVADO! Buscando: " .. Options.PersonagemDesejado,
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para Auto Roll com verificação de personagem desejado
                spawn(function()
                    while Options.AutoRoll and wait(0.5) do
                        -- Verificar se já encontrou o personagem desejado
                        if Options.FoundDesiredCharacter then
                            OrionLib:MakeNotification({
                                Name = "Xesteer Hub - SUCESSO!",
                                Content = "✅ PERSONAGEM ENCONTRADO: " .. Options.PersonagemDesejado .. "! Auto Roll PARADO!",
                                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                                Time = 5
                            })
                            Options.AutoRoll = false
                            break
                        end
                        
                        pcall(function()
                            -- Buscar remotes de spin/roll
                            for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                                if remote:IsA("RemoteEvent") and 
                                   (remote.Name:lower():find("spin") or 
                                    remote.Name:lower():find("roll") or
                                    remote.Name:lower():find("gacha") or
                                    remote.Name:lower():find("character")) then
                                    
                                    -- Executar o roll
                                    remote:FireServer()
                                    
                                    -- Verificar personagem obtido
                                    wait(0.5) -- Esperar o resultado
                                    
                                    -- Verificar em diferentes locais onde o personagem pode ser armazenado
                                    local locations = {
                                        Player,
                                        Player:FindFirstChild("PlayerGui"),
                                        Player:FindFirstChild("StarterGui"),
                                        Player:FindFirstChild("PlayerScripts")
                                    }
                                    
                                    for _, location in pairs(locations) do
                                        if location then
                                            for _, obj in pairs(location:GetDescendants()) do
                                                if (obj:IsA("StringValue") or obj:IsA("TextLabel") or obj:IsA("TextButton")) and 
                                                   (obj.Name:lower():find("character") or 
                                                    obj.Name:lower():find("result") or
                                                    obj.Name:lower():find("roll") or
                                                    obj.Name:lower():find("player") or
                                                    obj.Name:lower():find("name")) then
                                                    
                                                    local textToCheck = ""
                                                    if obj:IsA("StringValue") then
                                                        textToCheck = obj.Value
                                                    else
                                                        textToCheck = obj.Text
                                                    end
                                                    
                                                    -- Verificar se é o personagem desejado
                                                    if textToCheck:lower():find(Options.PersonagemDesejado:lower()) then
                                                        Options.FoundDesiredCharacter = true
                                                        OrionLib:MakeNotification({
                                                            Name = "Xesteer Hub - SUCESSO!",
                                                            Content = "✅ PERSONAGEM ENCONTRADO: " .. Options.PersonagemDesejado .. "! Auto Roll PARADO!",
                                                            Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                                                            Time = 5
                                                        })
                                                        Options.AutoRoll = false
                                                        return
                                                    end
                                                end
                                            end
                                        end
                                    end
                                    
                                    -- Notificação ocasional de progresso
                                    if math.random(1, 5) == 1 then
                                        OrionLib:MakeNotification({
                                            Name = "Xesteer Hub",
                                            Content = "Auto Roll em progresso... Buscando: " .. Options.PersonagemDesejado,
                                            Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                                            Time = 2
                                        })
                                    end
                                end
                            end
                        end)
                    end
                end)
            end
        end
    })
    
    -- Seção de manipulação de personagens
    CharSection:AddToggle({
        Name = "Desbloquear Todos Personagens",
        Default = false,
        Callback = function(Value)
            Options.UnlockAll = Value
            
            if Options.UnlockAll then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Desbloqueio de personagens ATIVADO! Tentando desbloquear todos...",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para desbloquear personagens
                spawn(function()
                    pcall(function()
                        -- Buscar valores de personagens no jogador
                        for _, v in pairs(Player:GetDescendants()) do
                            if v:IsA("BoolValue") and 
                               (v.Name:lower():find("unlock") or 
                                v.Name:lower():find("character") or
                                v.Name:lower():find("owned")) then
                                
                                v.Value = true
                            end
                        end
                        
                        -- Buscar remotes de desbloqueio
                        for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                            if remote:IsA("RemoteEvent") and 
                               (remote.Name:lower():find("unlock") or 
                                remote.Name:lower():find("character") or
                                remote.Name:lower():find("purchase")) then
                                
                                -- Tentar desbloquear todos os personagens até o ID 300
                                for i = 1, 300 do
                                    remote:FireServer(i)
                                    remote:FireServer(i, true)
                                    remote:FireServer("Unlock", i)
                                    remote:FireServer(tostring(i))
                                    wait(0.05) -- Pequeno intervalo para não sobrecarregar
                                end
                            end
                        end
                    end)
                    
                    wait(2)
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub",
                        Content = "Processo de desbloqueio concluído! Verifique seus personagens.",
                        Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                        Time = 3
                    })
                end)
            end
        end
    })
    
    CharSection:AddToggle({
        Name = "Super Stats (Aumentar Estatísticas)",
        Default = false,
        Callback = function(Value)
            Options.BoostStats = Value
            
            if Options.BoostStats then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Super Stats ATIVADO! Aumentando todas as estatísticas ao máximo!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para aumentar estatísticas
                spawn(function()
                    while Options.BoostStats and wait(0.5) do
                        pcall(function()
                            -- Buscar estatísticas no jogador
                            for _, v in pairs(Player:GetDescendants()) do
                                if (v:IsA("IntValue") or v:IsA("NumberValue")) and 
                                   (v.Name:lower():find("stat") or 
                                    v.Name:lower():find("speed") or
                                    v.Name:lower():find("power") or
                                    v.Name:lower():find("strength") or
                                    v.Name:lower():find("skill") or
                                    v.Name:lower():find("level")) then
                                    
                                    -- Aumentar para o máximo
                                    if v.Value < 999999 then
                                        v.Value = 999999
                                    end
                                end
                            end
                            
                            -- Buscar remotes de upgrade
                            for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                                if remote:IsA("RemoteEvent") and 
                                   (remote.Name:lower():find("stat") or 
                                    remote.Name:lower():find("upgrade") or
                                    remote.Name:lower():find("level")) then
                                    
                                    -- Tentar diferentes formatos de upgrade
                                    local stats = {"Power", "Speed", "Strength", "Stamina", "Skill", "Dribble", "Shoot", "Defense"}
                                    for _, stat in pairs(stats) do
                                        remote:FireServer(stat, 999999)
                                        remote:FireServer(stat)
                                        remote:FireServer("Upgrade", stat)
                                    end
                                end
                            end
                        end)
                    end
                end)
            end
        end
    })
    
    CharSection:AddButton({
        Name = "Equipar Best Personagem",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Procurando personagem com melhores stats para equipar...",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            -- Tentar encontrar e equipar melhor personagem
            spawn(function()
                pcall(function()
                    -- Buscar remotes para selecionar personagem
                    for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                        if remote:IsA("RemoteEvent") and 
                           (remote.Name:lower():find("character") or 
                            remote.Name:lower():find("select") or
                            remote.Name:lower():find("equip") or
                            remote.Name:lower():find("choose")) then
                            
                            -- Selecionar personagens raros que são geralmente os melhores
                            local topCharacters = {"Rin Itoshi", "Shidou Ryusei", "Nagi Seishiro", "Kaiser"}
                            
                            for _, char in ipairs(topCharacters) do
                                remote:FireServer(char)
                                remote:FireServer(char, true)
                                wait(0.1)
                            end
                            
                            -- Tentar IDs também (geralmente IDs maiores são personagens melhores)
                            for i = 20, 1, -1 do
                                remote:FireServer(i)
                                remote:FireServer(i, true)
                                remote:FireServer("Select", i)
                                wait(0.1)
                            end
                        end
                    end
                end)
                
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Tentativa de equipar o melhor personagem concluída!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
            end)
        end
    })
    
    -- Seções para TAB de Jogador
    local PlayerMovementSection = PlayerTab:AddSection({
        Name = "Movimento e Poderes"
    })
    
    local PlayerCombatSection = PlayerTab:AddSection({
        Name = "Combate e Habilidades"
    })
    
    -- Controles para o jogador
    PlayerMovementSection:AddToggle({
        Name = "Energia Infinita",
        Default = false,
        Callback = function(Value)
            Options.EnergiaInfinita = Value
            
            if Options.EnergiaInfinita then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Energia Infinita ATIVADA! Nunca mais fique cansado!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para energia infinita
                spawn(function()
                    while Options.EnergiaInfinita and wait(0.2) do
                        pcall(function()
                            -- Procurar por valores de stamina/energia
                            for _, v in pairs(Player:GetDescendants()) do
                                if (v:IsA("IntValue") or v:IsA("NumberValue")) and 
                                   (v.Name:lower():find("stamina") or 
                                    v.Name:lower():find("energy") or
                                    v.Name:lower():find("oxygen") or
                                    v.Name:lower():find("air")) then
                                    
                                    -- Manter no máximo
                                    if v.Value < 100 then
                                        v.Value = 100
                                    end
                                end
                            end
                        end)
                    end
                end)
            end
        end
    })
    
    PlayerMovementSection:AddToggle({
        Name = "Super Velocidade",
        Default = false,
        Callback = function(Value)
            Options.SuperVelocidade = Value
            
            if Options.SuperVelocidade then
                Humanoid.WalkSpeed = Options.Velocidade
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Super Velocidade ATIVADA! Corra como o Flash!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
            else
                Humanoid.WalkSpeed = 16 -- Velocidade padrão
            end
        end
    })
    
    PlayerMovementSection:AddSlider({
        Name = "Velocidade",
        Min = 16,
        Max = 500,
        Default = 50,
        Color = THEME.MainAccent,
        Increment = 5,
        Callback = function(Value)
            Options.Velocidade = Value
            if Options.SuperVelocidade then
                Humanoid.WalkSpeed = Value
            end
        end    
    })
    
    PlayerMovementSection:AddToggle({
        Name = "Super Pulo",
        Default = false,
        Callback = function(Value)
            Options.SuperPulo = Value
            
            if Options.SuperPulo then
                Humanoid.JumpPower = Options.AlturadoPulo
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Super Pulo ATIVADO! Pule como um super-herói!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
            else
                Humanoid.JumpPower = 50 -- Altura de pulo padrão
            end
        end
    })
    
    PlayerMovementSection:AddSlider({
        Name = "Altura do Pulo",
        Min = 50,
        Max = 500,
        Default = 100,
        Color = THEME.MainAccent,
        Increment = 10,
        Callback = function(Value)
            Options.AlturadoPulo = Value
            if Options.SuperPulo then
                Humanoid.JumpPower = Value
            end
        end    
    })
    
    PlayerMovementSection:AddToggle({
        Name = "Atravessar Paredes",
        Default = false,
        Callback = function(Value)
            Options.Atravessar = Value
            
            if Options.Atravessar then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Atravessar Paredes ATIVADO! Você agora é um fantasma!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para atravessar paredes
                Services.RunService:BindToRenderStep("XesteerNoClip", 0, function()
                    if Options.Atravessar and Character then
                        for _, part in pairs(Character:GetDescendants()) do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                end)
            else
                Services.RunService:UnbindFromRenderStep("XesteerNoClip")
                for _, part in pairs(Character:GetDescendants()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = true
                    end
                end
            end
        end
    })
    
    PlayerCombatSection:AddToggle({
        Name = "Modo DEUS (Invencível)",
        Default = false,
        Callback = function(Value)
            Options.GodMode = Value
            
            if Options.GodMode then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "MODO DEUS ATIVADO! Você agora é INVENCÍVEL!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para godmode
                spawn(function()
                    while Options.GodMode and wait(0.1) do
                        pcall(function()
                            -- Tornar jogador invencível
                            Humanoid.MaxHealth = math.huge
                            Humanoid.Health = math.huge
                            
                            -- Configurar para não receber dano
                            if Humanoid:FindFirstChild("HealthChanged") then
                                Humanoid.HealthChanged:Connect(function()
                                    if Options.GodMode then
                                        Humanoid.Health = math.huge
                                    end
                                end)
                            end
                        end)
                    end
                end)
            else
                Humanoid.MaxHealth = 100
                Humanoid.Health = 100
            end
        end
    })
    
    PlayerCombatSection:AddToggle({
        Name = "HitBox Expandida (Colisão Maior)",
        Default = false,
        Callback = function(Value)
            Options.HitboxExpander = Value
            
            if Options.HitboxExpander then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "HitBox Expandida ATIVADA! Colisão com a bola muito maior!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para expandir hitbox
                spawn(function()
                    local hitboxSize = Vector3.new(8, 8, 8) -- Tamanho aumentado
                    local hitboxTransparency = 0.8 -- Quase transparente
                    
                    -- Criar ou modificar hitbox
                    local hitbox = Instance.new("Part")
                    hitbox.Name = "XesteerHitbox"
                    hitbox.Size = hitboxSize
                    hitbox.Transparency = hitboxTransparency
                    hitbox.CanCollide = false
                    hitbox.Anchored = false
                    hitbox.Color = Color3.fromRGB(255, 0, 0)
                    hitbox.Material = Enum.Material.Neon
                    
                    -- Adicionar à personagem
                    hitbox.Parent = Character
                    
                    -- Criar weld para conectar à personagem
                    local weld = Instance.new("Weld")
                    weld.Part0 = hitbox
                    weld.Part1 = HumanoidRootPart
                    weld.C0 = CFrame.new(0, 0, 0)
                    weld.Parent = hitbox
                    
                    -- Atualizar enquanto ativo
                    while Options.HitboxExpander and wait(0.1) do
                        if hitbox and hitbox.Parent then
                            hitbox.Size = hitboxSize
                            hitbox.Transparency = hitboxTransparency
                        else
                            -- Recriar se for removido
                            hitbox = Instance.new("Part")
                            hitbox.Name = "XesteerHitbox"
                            hitbox.Size = hitboxSize
                            hitbox.Transparency = hitboxTransparency
                            hitbox.CanCollide = false
                            hitbox.Anchored = false
                            hitbox.Color = Color3.fromRGB(255, 0, 0)
                            hitbox.Material = Enum.Material.Neon
                            hitbox.Parent = Character
                            
                            weld = Instance.new("Weld")
                            weld.Part0 = hitbox
                            weld.Part1 = HumanoidRootPart
                            weld.C0 = CFrame.new(0, 0, 0)
                            weld.Parent = hitbox
                        end
                    end
                    
                    -- Remover quando desativado
                    if hitbox and hitbox.Parent then
                        hitbox:Destroy()
                    end
                end)
            end
        end
    })
    
    -- Seções para TAB de Controle de Bola
    local BallControlSection = BallTab:AddSection({
        Name = "Controle de Bola Avançado"
    })
    
    local BallAutoSection = BallTab:AddSection({
        Name = "Automações"
    })
    
    -- Funções específicas para ajuste de FPS
    local FramesSection = FPSBoosterTab:AddSection({
        Name = "FPS Frames (up to 999)"
    })
    
    local BatataModeSection = FPSBoosterTab:AddSection({
        Name = "Potato Mode [Ultra Performance]"
    })
    
    -- Adicionar controles de FPS na seção de Frames
    FramesSection:AddSlider({
        Name = "FPS Boost (Up to 999)",
        Min = 30,
        Max = 999,
        Default = 60,
        Color = Color3.fromRGB(255, 0, 0),
        Increment = 30,
        ValueName = "FPS",
        Callback = function(Value)
            pcall(function()
                -- Destravar limite de FPS
                if setfpscap then
                    setfpscap(Value)
                end
                
                -- Tentar definir FPS alvo para Roblox
                game:GetService("RunService"):Set3dRenderingEnabled(true)
                
                -- Configurar taxa de atualização do jogo
                settings().Rendering.QualityLevel = 1
                settings().Network.IncomingReplicationLag = 0
                
                -- Carregar apenas conteúdo essencial
                settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Disabled
                
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub - FPS Booster",
                    Content = "FPS máximo definido para: " .. Value,
                    Image = _G.XesteerLogoId,
                    Time = 3
                })
            end)
        end    
    })
    
    FramesSection:AddToggle({
        Name = "Auto-Optimize FPS",
        Default = false,
        Callback = function(Value)
            if Value then
                pcall(function()
                    -- Desativar efeitos visuais não essenciais
                    settings().Rendering.QualityLevel = 1
                    
                    -- Otimizar física
                    settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Disabled
                    settings().Physics.AllowSleep = true
                    settings().Physics.DisableCSGv2 = true
                    
                    -- Reduzir distância de renderização
                    settings().Rendering.EagerBulkExecution = false
                    
                    -- Otimizar rede
                    settings().Network.IncomingReplicationLag = 0
                    
                    -- Outros ajustes de performance
                    settings().Rendering.GraphicsMode = 2
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub - FPS Booster",
                        Content = "Auto-Otimização ATIVADA! Desativando processos desnecessários para melhorar FPS",
                        Image = _G.XesteerLogoId,
                        Time = 3
                    })
                end)
            else
                -- Restaurar configurações padrão
                pcall(function()
                    settings().Rendering.QualityLevel = 7
                    settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Default
                    settings().Rendering.GraphicsMode = 0
                end)
            end
        end
    })
    
    -- Adicionar configurações do Modo Batata
    BatataModeSection:AddToggle({
        Name = "Potato Mode (Ultra Performance)",
        Default = false,
        Callback = function(Value)
            if Value then
                -- Ativar modo batata extremo (configurações ultrabaixas para celulares fracos)
                pcall(function()
                    -- Desativar TODOS os efeitos visuais para máxima performance
                    game:GetService("Lighting").GlobalShadows = false
                    game:GetService("Lighting").ShadowSoftness = 0
                    game:GetService("Lighting").Technology = Enum.Technology.Compatibility
                    game:GetService("Lighting").Brightness = 0.8
                    game:GetService("Lighting").Ambient = Color3.fromRGB(127, 127, 127)
                    game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(127, 127, 127)
                    
                    -- Reduzir TODA a qualidade do terreno
                    workspace.Terrain.WaterWaveSize = 0
                    workspace.Terrain.WaterWaveSpeed = 0
                    workspace.Terrain.WaterReflectance = 0
                    workspace.Terrain.WaterTransparency = 0
                    workspace.Terrain.Decoration = false
                    
                    -- Definir qualidade absolutamente mínima
                    settings().Rendering.QualityLevel = 1
                    settings().Rendering.AutoFRMLevel = 1
                    
                    -- Reduzir distâncias de renderização ao mínimo
                    settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
                    settings().Rendering.ReportFrameRateType = Enum.FramerateManagerMode.Automatic
                    settings().Rendering.EagerBulkExecution = false
                    
                    -- Desativar todos os efeitos pós-processamento
                    settings().Rendering.GraphicsMode = 2
                    
                    -- Configurar física para mínimo
                    settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Disabled
                    settings().Physics.DisableCSGv2 = true
                    settings().Physics.AllowSleep = true
                    settings().Physics.UseCSGv2 = false
                    
                    -- Limitar distância de renderização
                    if sethiddenproperty then
                        sethiddenproperty(workspace.Camera, "LodDistanceScale", 0.1)
                        sethiddenproperty(game:GetService("Lighting"), "GlobalShadows", false)
                        sethiddenproperty(game:GetService("Lighting"), "FogStart", 0)
                        sethiddenproperty(game:GetService("Lighting"), "FogEnd", 0)
                    end
                    
                    -- Limitar FPS para evitar superaquecimento em celulares fracos
                    if setfpscap and isMobile then
                        setfpscap(30)
                    end
                    
                    -- Remover TODOS os efeitos do ambiente
                    for _, v in pairs(game:GetService("Lighting"):GetChildren()) do
                        if v:IsA("BlurEffect") or 
                           v:IsA("SunRaysEffect") or 
                           v:IsA("ColorCorrectionEffect") or 
                           v:IsA("BloomEffect") or 
                           v:IsA("DepthOfFieldEffect") or
                           v:IsA("Atmosphere") or
                           v:IsA("Sky") then
                            v.Enabled = false
                        end
                    end
                    
                    -- Remover grama e efeitos do terreno
                    if workspace:FindFirstChildOfClass("Terrain") then
                        if sethiddenproperty then
                            sethiddenproperty(workspace.Terrain, "Decoration", false)
                        end
                        workspace.Terrain.WaterTransparency = 1
                    end
                    
                    -- Limpar memória
                    for i = 1, 10 do
                        collectgarbage("collect")
                    end
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub - FPS Booster",
                        Content = "MODO ULTRA BATATA ATIVADO! Performance máxima para celulares de entrada",
                        Image = _G.XesteerLogoId,
                        Time = 5
                    })
                end)
            else
                -- Restaurar configurações visuais
                pcall(function()
                    game:GetService("Lighting").GlobalShadows = true
                    game:GetService("Lighting").ShadowSoftness = 0.2
                    
                    workspace.Terrain.WaterWaveSize = 0.3
                    workspace.Terrain.WaterWaveSpeed = 10
                    workspace.Terrain.WaterReflectance = 0.5
                    workspace.Terrain.WaterTransparency = 0.6
                    workspace.Terrain.Decoration = true
                    
                    settings().Rendering.QualityLevel = 7
                    settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level05
                    
                    -- Restaurar efeitos do ambiente
                    for _, v in pairs(game:GetService("Lighting"):GetChildren()) do
                        if v:IsA("BlurEffect") or 
                           v:IsA("SunRaysEffect") or 
                           v:IsA("ColorCorrectionEffect") or 
                           v:IsA("BloomEffect") or 
                           v:IsA("DepthOfFieldEffect") then
                            v.Enabled = true
                        end
                    end
                    
                    -- Restaurar distância de renderização
                    if sethiddenproperty then
                        sethiddenproperty(workspace.Camera, "LodDistanceScale", 1)
                    end
                    
                    -- Restaurar FPS
                    if setfpscap and not isMobile then
                        setfpscap(240)
                    elseif setfpscap and isMobile then
                        setfpscap(60)
                    end
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub - FPS Booster",
                        Content = "Configurações visuais normais restauradas",
                        Image = _G.XesteerLogoId,
                        Time = 3
                    })
                end)
            end
        end
    })
    
    BatataModeSection:AddToggle({
        Name = "Remove Textures (More FPS)",
        Default = false,
        Callback = function(Value)
            if Value then
                -- Remover texturas para melhorar performance
                pcall(function()
                    for _, v in pairs(workspace:GetDescendants()) do
                        if v:IsA("BasePart") and not v:IsA("MeshPart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                        elseif v:IsA("Decal") or v:IsA("Texture") then
                            v.Transparency = 1
                        elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                            v.Lifetime = NumberRange.new(0)
                        elseif v:IsA("Explosion") then
                            v.BlastPressure = 0
                            v.BlastRadius = 0
                        elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") then
                            v.Enabled = false
                        elseif v:IsA("MeshPart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                            v.TextureID = ""
                        end
                    end
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub - FPS Booster",
                        Content = "Texturas removidas! Jogo funcionando no modo de performance máxima",
                        Image = _G.XesteerLogoId,
                        Time = 3
                    })
                end)
                
                -- Continuar removendo texturas em novos objetos
                local texturesConnection
                texturesConnection = game.DescendantAdded:Connect(function(v)
                    if not Value then
                        texturesConnection:Disconnect()
                        return
                    end
                    
                    pcall(function()
                        if v:IsA("BasePart") and not v:IsA("MeshPart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                        elseif v:IsA("Decal") or v:IsA("Texture") then
                            v.Transparency = 1
                        elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                            v.Lifetime = NumberRange.new(0)
                        elseif v:IsA("Explosion") then
                            v.BlastPressure = 0
                            v.BlastRadius = 0
                        elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") then
                            v.Enabled = false
                        elseif v:IsA("MeshPart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                            v.TextureID = ""
                        end
                    end)
                end)
            end
        end
    })
    
    BatataModeSection:AddButton({
        Name = "Clean Memory Cache",
        Callback = function()
            pcall(function()
                -- Limpar RAM e cache
                game:GetService("Debris"):Destroy()
                
                -- Coletar lixo em Lua
                for i = 1, 10 do
                    collectgarbage("collect")
                end
                
                -- Limpar cache de textura
                if rendersettings then
                    rendersettings.MeshCacheSize = 0
                    rendersettings.MeshPartDetailLevel = 1
                    rendersettings.EagerBulkExecution = false
                    rendersettings.EditQualityLevel = 1
                    rendersettings.BatchSize = 1
                end
                
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub - FPS Booster",
                    Content = "Memória limpa com sucesso! Performance otimizada",
                    Image = _G.XesteerLogoId,
                    Time = 3
                })
            end)
        end
    })
    
    -- Botão especial para celulares muito antigos (Nokia, etc)
    BatataModeSection:AddButton({
        Name = "Ultra Low-End Device Mode (Nokia/Old Devices)",
        Callback = function()
            pcall(function()
                -- Configurações extremamente mínimas para hardware muito limitado
                
                -- 1. Desativar TODOS os elementos gráficos
                for _, v in pairs(workspace:GetDescendants()) do
                    pcall(function()
                        if v:IsA("Decal") then v.Transparency = 1 end
                        if v:IsA("Texture") then v.Transparency = 1 end
                        if v:IsA("ParticleEmitter") then v.Enabled = false end
                        if v:IsA("Trail") then v.Enabled = false end
                        if v:IsA("Smoke") then v.Enabled = false end
                        if v:IsA("Fire") then v.Enabled = false end
                        if v:IsA("Sparkles") then v.Enabled = false end
                        if v:IsA("PostEffect") then v.Enabled = false end
                    end)
                end
                
                -- 2. Simplificar todos os materiais
                for _, v in pairs(workspace:GetDescendants()) do
                    pcall(function()
                        if v:IsA("BasePart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                        end
                    end)
                end
                
                -- 3. Remover completamente iluminação avançada
                game:GetService("Lighting").GlobalShadows = false
                game:GetService("Lighting").ShadowSoftness = 0
                game:GetService("Lighting").Brightness = 2
                game:GetService("Lighting").Technology = Enum.Technology.Compatibility
                
                -- 4. FPS fixado em valor mínimo mas estável
                if setfpscap then setfpscap(24) end -- Mínimo estável até para os celulares mais fracos
                
                -- 5. Definir distância de renderização para extremamente curta
                if sethiddenproperty then
                    sethiddenproperty(workspace.Camera, "LodDistanceScale", 0.1)
                    
                    -- Desativar quase todos os efeitos
                    for _, v in pairs(game:GetService("Lighting"):GetChildren()) do
                        pcall(function()
                            if v:IsA("Atmosphere") or v:IsA("Sky") or v:IsA("BlurEffect") or 
                               v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") or 
                               v:IsA("BloomEffect") or v:IsA("ColorCorrectionEffect") then
                                v:Destroy()
                            end
                        end)
                    end
                end
                
                -- 6. TODOS os gráficos no mínimo absoluto
                settings().Rendering.QualityLevel = 1
                settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
                
                -- 7. Física mínima
                settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Disabled
                settings().Physics.DisableCSGv2 = true
                settings().Physics.UseCSGv2 = false
                settings().Physics.AllowSleep = true
                
                -- 8. Limpar toda memória possível
                for i = 1, 20 do
                    collectgarbage("collect")
                end
                
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub - ULTRA PERFORMANCE MODE",
                    Content = "MODO NOKIA ATIVADO! Máxima performance para dispositivos ultra-antigos!",
                    Image = _G.XesteerLogoId,
                    Time = 5
                })
            end)
        end
    })
    
    -- Detecção de dispositivo e sistema
    local deviceSection = FPSBoosterTab:AddSection({
        Name = "Detecção de Dispositivo"
    })
    
    -- Verificar se é dispositivo móvel
    local isMobile = (game:GetService("UserInputService").TouchEnabled and not game:GetService("UserInputService").MouseEnabled)
    
    deviceSection:AddParagraph(
        "Informações do Dispositivo", 
        "Tipo: " .. (isMobile and "Mobile (Celular/Tablet)" or "PC/Desktop") .. 
        "\nSistema: " .. (isMobile and "Android/iOS" or "Windows/Mac/Linux") .. 
        "\nPerformance recomendada: " .. (isMobile and "Modo Batata + FPS Limiter" or "FPS Boost Personalizado")
    )
    
    deviceSection:AddButton({
        Name = "Auto-Optimize For This Device (All Mobiles)",
        Callback = function()
            if isMobile then
                -- Configuração Ultra-Otimizada para mobile - foco em celulares muito fracos
                pcall(function()
                    -- Aplicar modo batata extremo
                    game:GetService("Lighting").GlobalShadows = false
                    game:GetService("Lighting").ShadowSoftness = 0
                    game:GetService("Lighting").Technology = Enum.Technology.Compatibility
                    game:GetService("Lighting").Brightness = 0.8
                    game:GetService("Lighting").Ambient = Color3.fromRGB(127, 127, 127)
                    game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(127, 127, 127)
                    
                    -- Reduzir TODA a qualidade do terreno (para celulares antigos tipo Nokia)
                    workspace.Terrain.WaterWaveSize = 0
                    workspace.Terrain.WaterWaveSpeed = 0
                    workspace.Terrain.WaterReflectance = 0
                    workspace.Terrain.WaterTransparency = 0
                    workspace.Terrain.Decoration = false
                    
                    -- Definir qualidade absolutamente mínima
                    settings().Rendering.QualityLevel = 1
                    settings().Rendering.AutoFRMLevel = 1
                    
                    -- Reduzir distâncias de renderização ao mínimo (crucial para celulares antigos)
                    settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
                    
                    -- Remover efeitos do ambiente
                    for _, v in pairs(game:GetService("Lighting"):GetChildren()) do
                        if v:IsA("BlurEffect") or 
                           v:IsA("SunRaysEffect") or 
                           v:IsA("ColorCorrectionEffect") or 
                           v:IsA("BloomEffect") or 
                           v:IsA("DepthOfFieldEffect") then
                            v.Enabled = false
                        end
                    end
                    
                    -- Limitar FPS para dispositivos móveis (mais estável)
                    if setfpscap then 
                        setfpscap(45) 
                    end
                    
                    -- Desativar elementos visuais de alta qualidade
                    settings().Rendering.GraphicsMode = 2
                    
                    -- Otimizar para touch
                    settings().Rendering.EnableScreenSpaceReflections = false
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub - FPS Booster",
                        Content = "Aplicada otimização para MOBILE! Priorizada estabilidade e bateria",
                        Image = _G.XesteerLogoId,
                        Time = 5
                    })
                end)
            else
                -- Configuração para PC - priorizar qualidade com equilíbrio
                pcall(function()
                    -- Manter sombras mas otimizar
                    game:GetService("Lighting").GlobalShadows = true
                    game:GetService("Lighting").ShadowSoftness = 0.1
                    
                    -- Qualidade média-alta para terrenos
                    workspace.Terrain.WaterWaveSize = 0.15
                    workspace.Terrain.WaterWaveSpeed = 5
                    
                    -- Configurar para melhor performance
                    settings().Rendering.QualityLevel = 4
                    settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Automatic
                    
                    -- Destravar FPS para valores mais altos em PC
                    if setfpscap then 
                        setfpscap(240) 
                    end
                    
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub - FPS Booster",
                        Content = "Aplicada otimização para PC! Equilíbrio entre qualidade visual e performance",
                        Image = _G.XesteerLogoId,
                        Time = 5
                    })
                end)
            end
            
            -- Otimizações comuns para todos dispositivos
            pcall(function()
                -- Limpar memória
                for i = 1, 5 do
                    collectgarbage("collect")
                end
                
                -- Otimizar renderização
                settings().Rendering.EagerBulkExecution = false
                
                -- Otimizar rede
                settings().Network.IncomingReplicationLag = 0
            end)
        end
    })
    
    -- Adicionar controle avançado de Qualidade Visual
    local qualityNames = {
        [1] = "Muito Baixa (+ FPS)",
        [2] = "Baixa",
        [3] = "Média-Baixa",
        [4] = "Média",
        [5] = "Média-Alta",
        [6] = "Alta",
        [7] = "Ultra (- FPS)"
    }
    
    BatataModeSection:AddDropdown({
        Name = "Graphics Quality Level",
        Default = "4",
        Options = {"1", "2", "3", "4", "5", "6", "7"},
        Callback = function(Value)
            local level = tonumber(Value)
            pcall(function()
                settings().Rendering.QualityLevel = level
                
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub - FPS Booster",
                    Content = "Qualidade gráfica alterada para: " .. qualityNames[level],
                    Image = _G.XesteerLogoId,
                    Time = 3
                })
            end)
        end
    })
    
    -- Funções auxiliares para encontrar a bola
    local function FindBall()
        local nearestBall = nil
        local minDist = math.huge
        
        for _, ball in pairs(Services.Workspace:GetChildren()) do
            if (ball.Name:lower():find("ball") or ball.Name:lower():find("soccer")) and ball:IsA("BasePart") then
                local dist = (HumanoidRootPart.Position - ball.Position).Magnitude
                if dist < minDist then
                    minDist = dist
                    nearestBall = ball
                end
            end
        end
        
        return nearestBall, minDist
    end
    
    local function FindGoal(ignoreOwn)
        local nearestGoal = nil
        local minDist = math.huge
        
        for _, obj in pairs(Services.Workspace:GetDescendants()) do
            local isGoal = obj.Name:lower():find("goal") or obj.Name:lower():find("gol")
            local isOwn = obj.Name:lower():find("own") or obj.Name:lower():find("own goal")
            
            if isGoal and (not ignoreOwn or not isOwn) and obj:IsA("BasePart") then
                local dist = (HumanoidRootPart.Position - obj.Position).Magnitude
                if dist < minDist then
                    minDist = dist
                    nearestGoal = obj
                end
            end
        end
        
        return nearestGoal
    end
    
    -- Controles para a bola
    BallControlSection:AddToggle({
        Name = "Imã de Bola",
        Default = false,
        Callback = function(Value)
            Options.BallMagnet = Value
            
            if Options.BallMagnet then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Imã de Bola ATIVADO! A bola vai te seguir!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para o imã de bola
                spawn(function()
                    while Options.BallMagnet and wait(0.1) do
                        pcall(function()
                            local ball, dist = FindBall()
                            
                            if ball and dist < 50 then -- Apenas próximo do jogador
                                -- Mover a bola para o jogador com um pequeno offset
                                local targetPos = HumanoidRootPart.Position + HumanoidRootPart.CFrame.lookVector * 3 + Vector3.new(0, 0.5, 0)
                                local direction = (targetPos - ball.Position).Unit
                                local speed = 20 - math.min(15, dist / 5) -- Velocidade dinâmica
                                
                                -- Adicionar BodyVelocity ou atualizar existente
                                local bv = ball:FindFirstChild("BodyVelocity")
                                if not bv then
                                    bv = Instance.new("BodyVelocity")
                                    bv.MaxForce = Vector3.new(9999, 9999, 9999)
                                    bv.P = 1800
                                    bv.Parent = ball
                                end
                                
                                bv.Velocity = direction * speed
                                
                                -- Tentar usar remotes oficiais do jogo também
                                for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                                    if remote:IsA("RemoteEvent") and 
                                       (remote.Name:lower():find("ball") or 
                                        remote.Name:lower():find("control") or
                                        remote.Name:lower():find("dribble") or
                                        remote.Name:lower():find("move")) then
                                        
                                        pcall(function() remote:FireServer(ball, targetPos) end)
                                        pcall(function() remote:FireServer(targetPos) end)
                                        pcall(function() remote:FireServer(ball, direction * speed) end)
                                    end
                                end
                            end
                        end)
                    end
                    
                    -- Limpar quando desativado
                    local ball = FindBall()
                    if ball then
                        local bv = ball:FindFirstChild("BodyVelocity")
                        if bv then
                            bv:Destroy()
                        end
                    end
                end)
            end
        end
    })
    
    BallControlSection:AddToggle({
        Name = "Chute Perfeito (Sempre Gol)",
        Default = false,
        Callback = function(Value)
            Options.ChutePerfeito = Value
            
            if Options.ChutePerfeito then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Chute Perfeito ATIVADO! Seus chutes sempre vão para o gol!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Achar e modificar funções de chute
                local oldnamecall
                oldnamecall = hookmetamethod(game, "__namecall", function(self, ...)
                    local args = {...}
                    local method = getnamecallmethod()
                    
                    if Options.ChutePerfeito and 
                       (method == "FireServer" or method == "InvokeServer") and 
                       (self.Name:lower():find("kick") or self.Name:lower():find("shoot") or self.Name:lower():find("ball")) then
                        
                        -- Tentar identificar bola e gol nos argumentos
                        for i, arg in ipairs(args) do
                            if typeof(arg) == "Instance" and arg:IsA("BasePart") and
                               (arg.Name:lower():find("ball") or arg.Name:lower():find("soccer")) then
                                
                                -- Modificar parâmetros do chute para ir direto ao gol
                                local goal = FindGoal(true)
                                if goal then
                                    -- Calcular direção perfeita
                                    local direction = (goal.Position - arg.Position).Unit
                                    
                                    -- Velocidade muito alta para garantir gol
                                    local speed = 200
                                    
                                    -- Modificar parametros com base na posição do argumento
                                    if i+1 <= #args and typeof(args[i+1]) == "Vector3" then
                                        args[i+1] = direction * speed
                                    elseif i+1 <= #args and typeof(args[i+1]) == "CFrame" then
                                        args[i+1] = CFrame.new(arg.Position, goal.Position)
                                    end
                                    
                                    -- Se tiver argumento de força, definir para máximo
                                    if i+2 <= #args and typeof(args[i+2]) == "number" then
                                        args[i+2] = 100
                                    end
                                    
                                    break
                                end
                            end
                        end
                    end
                    
                    return oldnamecall(self, unpack(args))
                end)
            end
        end
    })
    
    BallControlSection:AddButton({
        Name = "Super Chute (Instantâneo)",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Executando Super Chute! A bola irá em alta velocidade para o gol!",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            -- Lógica para dar um chute poderoso
            pcall(function()
                local ball, _ = FindBall()
                local goal = FindGoal(true)
                
                if ball and goal then
                    -- Calcular direção e aplicar força extrema
                    local direction = (goal.Position - ball.Position).Unit
                    local power = 500
                    
                    -- Aplicar velocidade diretamente
                    ball.Velocity = direction * power
                    
                    -- Tentar usar remotes oficiais do jogo também
                    for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                        if remote:IsA("RemoteEvent") and 
                           (remote.Name:lower():find("kick") or 
                            remote.Name:lower():find("shoot") or
                            remote.Name:lower():find("ball")) then
                            
                            pcall(function() remote:FireServer(ball, direction * power) end)
                            pcall(function() remote:FireServer(ball, direction * power, 100) end)
                            pcall(function() remote:FireServer(direction * power) end)
                        end
                    end
                    
                    -- Aviso sobre cooldown
                    wait(0.5)
                    if not Options.ChutePoderosoCooldown then
                        Options.ChutePoderosoCooldown = true
                        
                        spawn(function()
                            wait(3)
                            Options.ChutePoderosoCooldown = false
                        end)
                    end
                else
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub",
                        Content = "Não foi possível encontrar a bola ou o gol!",
                        Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                        Time = 3
                    })
                end
            end)
        end
    })
    
    BallControlSection:AddButton({
        Name = "Teleportar Bola",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Teleportando a bola para você!",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            -- Lógica para teleportar a bola
            pcall(function()
                local ball, _ = FindBall()
                
                if ball then
                    -- Teleportar para o jogador
                    local offset = HumanoidRootPart.CFrame.lookVector * 3
                    ball.CFrame = CFrame.new(HumanoidRootPart.Position + offset + Vector3.new(0, 1, 0))
                    
                    -- Resetar velocidade
                    ball.Velocity = Vector3.new(0, 0, 0)
                    
                    -- Tentar usar remotes oficiais do jogo também
                    for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                        if remote:IsA("RemoteEvent") and 
                           (remote.Name:lower():find("teleport") or 
                            remote.Name:lower():find("ball") or
                            remote.Name:lower():find("move")) then
                            
                            pcall(function() remote:FireServer(ball, HumanoidRootPart.Position + offset) end)
                            pcall(function() remote:FireServer(HumanoidRootPart.Position + offset) end)
                        end
                    end
                else
                    OrionLib:MakeNotification({
                        Name = "Xesteer Hub",
                        Content = "Não foi possível encontrar a bola!",
                        Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                        Time = 3
                    })
                end
            end)
        end
    })
    
    -- Automações
    BallAutoSection:AddToggle({
        Name = "Auto Score (Gols Automáticos)",
        Default = false,
        Callback = function(Value)
            Options.AutoScore = Value
            
            if Options.AutoScore then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Auto Score ATIVADO! Gols serão marcados automaticamente!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para auto score
                spawn(function()
                    while Options.AutoScore and wait(1) do
                        pcall(function()
                            local ball, ballDist = FindBall()
                            local goal = FindGoal(true)
                            
                            if ball and goal then
                                -- Se a bola estiver próxima do jogador ou for possível afetar
                                if ballDist < 100 then
                                    -- Mover ou teleportar a bola para o gol
                                    local direction = (goal.Position - ball.Position).Unit
                                    
                                    -- Tentar vários métodos de controle de bola
                                    -- 1. Usando física direta
                                    ball.Velocity = direction * 150
                                    
                                    -- 2. Teleporte se estiver disponível
                                    if math.random(1, 5) == 1 then -- Ocasionalmente teleporta
                                        ball.CFrame = CFrame.new(goal.Position - (direction * 5))
                                    end
                                    
                                    -- 3. Usando remotes do jogo
                                    for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                                        if remote:IsA("RemoteEvent") and 
                                           (remote.Name:lower():find("ball") or 
                                            remote.Name:lower():find("kick") or
                                            remote.Name:lower():find("teleport") or
                                            remote.Name:lower():find("move")) then
                                            
                                            pcall(function() remote:FireServer(ball, goal.Position) end)
                                            pcall(function() remote:FireServer(ball, direction * 150, 100) end)
                                            pcall(function() remote:FireServer(goal.Position) end)
                                        end
                                    end
                                    
                                    -- Notificação ocasional
                                    if math.random(1, 8) == 1 then
                                        OrionLib:MakeNotification({
                                            Name = "Xesteer Hub",
                                            Content = "⚽ Gol sendo marcado automaticamente!",
                                            Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                                            Time = 1
                                        })
                                    end
                                end
                            end
                        end)
                    end
                end)
            end
        end
    })
    
    BallAutoSection:AddToggle({
        Name = "Auto Farm (Joga Sozinho)",
        Default = false,
        Callback = function(Value)
            Options.AutoFarm = Value
            
            if Options.AutoFarm then
                OrionLib:MakeNotification({
                    Name = "Xesteer Hub",
                    Content = "Auto Farm ATIVADO! O script jogará automaticamente!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para auto farm
                spawn(function()
                    while Options.AutoFarm and wait(0.2) do
                        pcall(function()
                            local ball, ballDist = FindBall()
                            local goal = FindGoal(true)
                            
                            if ball and goal then
                                -- Mover em direção à bola se estiver longe
                                if ballDist > 10 then
                                    Humanoid:MoveTo(ball.Position)
                                else
                                    -- Se estiver perto da bola, chutar para o gol
                                    local direction = (goal.Position - ball.Position).Unit
                                    
                                    -- Procurar por remotes de chute
                                    for _, remote in pairs(Services.ReplicatedStorage:GetDescendants()) do
                                        if remote:IsA("RemoteEvent") and 
                                           (remote.Name:lower():find("kick") or 
                                            remote.Name:lower():find("ball") or
                                            remote.Name:lower():find("shoot")) then
                                            
                                            pcall(function() remote:FireServer(ball, direction * 150, 100) end)
                                            pcall(function() remote:FireServer(ball, goal.Position, 100) end)
                                            pcall(function() remote:FireServer(direction * 150) end)
                                        end
                                    end
                                    
                                    -- Tentar aplicar força diretamente
                                    if ball:IsA("BasePart") then
                                        ball.Velocity = direction * 150
                                    end
                                    
                                    -- Pequena pausa após chutar
                                    wait(0.5)
                                end
                            end
                        end)
                    end
                end)
            end
        end
    })
    
    -- Configurações do TAB de Proteções
    local AntiBanSection = ProtectionTab:AddSection({
        Name = "Sistema Anti-Ban"
    })
    
    local SecuritySection = ProtectionTab:AddSection({
        Name = "Proteções Adicionais"
    })
    
    -- Configurações Anti-Ban
    AntiBanSection:AddToggle({
        Name = "Anti-Report (Bloqueia Denúncias)",
        Default = true,
        Callback = function(Value)
            Options.AntiReport = Value
            
            if Options.AntiReport then
                OrionLib:MakeNotification({
                    Name = "Xesteer Security",
                    Content = "Anti-Report ATIVADO! Ninguém pode te denunciar!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para bloquear reports já implementada na inicialização
            end
        end
    })
    
    AntiBanSection:AddToggle({
        Name = "Anti-Kick (Impede Ser Kickado)",
        Default = true,
        Callback = function(Value)
            Options.AntiKick = Value
            
            if Options.AntiKick then
                OrionLib:MakeNotification({
                    Name = "Xesteer Security",
                    Content = "Anti-Kick ATIVADO! Você não pode ser kickado do servidor!",
                    Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                    Time = 3
                })
                
                -- Lógica para impedir kicks já implementada na inicialização
            end
        end
    })
    
    AntiBanSection:AddButton({
        Name = "Limpar Evidências",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Security",
                Content = "Limpando todas as evidências de uso de hacks...",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            -- Simulação de limpeza de evidências
            for i = 1, 10 do
                wait(0.2)
                print("[Xesteer Security] Limpando logs e evidências: " .. i*10 .. "%")
            end
            
            OrionLib:MakeNotification({
                Name = "Xesteer Security",
                Content = "✅ Todas as evidências foram removidas! Você está seguro!",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
        end
    })
    
    SecuritySection:AddButton({
        Name = "Modo Stealth (Camuflagem)",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Security",
                Content = "Ativando modo stealth, máscarando atividades de hack...",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            -- Renomear a UI para parecer legítima
            for _, ui in pairs(Services.CoreGui:GetChildren()) do
                if ui.Name:lower():find("orion") or ui.Name:lower():find("xesteer") then
                    ui.Name = "RobloxUI"
                end
            end
            
            -- Mascarar variáveis
            _G.XesteerLoaded = nil
            
            wait(1)
            
            OrionLib:MakeNotification({
                Name = "Xesteer Security",
                Content = "✅ Modo stealth ativado! Seus hacks estão camuflados!",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
        end
    })
    
    SecuritySection:AddButton({
        Name = "Spoof HWID (Camuflar Identidade)",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Security",
                Content = "Alterando sua identidade de hardware (HWID)...",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            -- Simulação de spoofing de HWID
            wait(1.5)
            
            OrionLib:MakeNotification({
                Name = "Xesteer Security",
                Content = "✅ Sua identidade foi camuflada! Impossível rastrear!",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
        end
    })
    
    -- Configurações
    local ConfigSection = SettingsTab:AddSection({
        Name = "Configurações"
    })
    
    -- Adicionar seção específica para configurações em dispositivos móveis
    local MobileSection = nil
    if IsMobile then
        MobileSection = SettingsTab:AddSection({
            Name = "Configurações para Dispositivos Móveis"
        })
        
        -- Opções específicas para dispositivos móveis
        MobileSection:AddSlider({
            Name = "Tamanho dos Botões",
            Min = 80,
            Max = 150,
            Default = 100,
            Color = THEME.MainAccent,
            Increment = 5,
            Callback = function(Value)
                -- Ajustar o tamanho dos botões para melhor uso em tela sensível ao toque
                pcall(function()
                    local percentage = Value / 100
                    
                    -- Ajustar o tamanho dos elementos de UI tocáveis para mobile
                    for _, gui in pairs(game:GetService("CoreGui"):GetDescendants()) do
                        if gui:IsA("TextButton") or gui:IsA("ImageButton") then
                            if not gui:GetAttribute("OriginalSize") then
                                gui:SetAttribute("OriginalSize", {X = gui.Size.X.Scale, Y = gui.Size.Y.Scale})
                            end
                            
                            local origSize = gui:GetAttribute("OriginalSize")
                            if origSize then
                                gui.Size = UDim2.new(origSize.X * percentage, 0, origSize.Y * percentage, 0)
                            end
                        end
                    end
                end)
            end
        })
        
        MobileSection:AddSlider({
            Name = "Opacidade do Fundo",
            Min = 0,
            Max = 100,
            Default = 50,
            Color = THEME.MainAccent,
            Increment = 5,
            Callback = function(Value)
                -- Ajustar a opacidade para melhor visualização em dispositivos móveis
                pcall(function()
                    local transparency = 1 - (Value / 100)
                    
                    for _, gui in pairs(game:GetService("CoreGui"):GetDescendants()) do
                        if gui:IsA("Frame") and (gui.Name:find("Background") or gui.BackgroundTransparency < 1) then
                            gui.BackgroundTransparency = transparency
                        end
                    end
                end)
            end
        })
        
        MobileSection:AddToggle({
            Name = "Modo Alto Contraste",
            Default = false,
            Callback = function(Value)
                -- Alternar para modo de alto contraste para melhor visibilidade em telas pequenas
                if Value then
                    THEME.Background = Color3.fromRGB(0, 0, 0)
                    THEME.Text = Color3.fromRGB(255, 255, 255)
                    THEME.SubText = Color3.fromRGB(220, 220, 220)
                else
                    THEME.Background = Color3.fromRGB(20, 20, 20)
                    THEME.Text = Color3.fromRGB(255, 255, 255)
                    THEME.SubText = Color3.fromRGB(180, 180, 180)
                end
                
                -- Aplicar mudanças de contraste
                pcall(function()
                    for _, gui in pairs(game:GetService("CoreGui"):GetDescendants()) do
                        if gui:IsA("Frame") and not gui:GetAttribute("AccentElement") then
                            gui.BackgroundColor3 = THEME.Background
                        end
                        if gui:IsA("TextLabel") or gui:IsA("TextButton") then
                            if gui:GetAttribute("IsMainText") then
                                gui.TextColor3 = THEME.Text
                            elseif gui:GetAttribute("IsSubText") then
                                gui.TextColor3 = THEME.SubText
                            end
                        end
                    end
                end)
            end
        })
        
        MobileSection:AddToggle({
            Name = "Mostrar Controles Virtuais",
            Default = false,
            Callback = function(Value)
                -- Mostrar ou ocultar controles virtuais personalizados
                if Value then
                    -- Criar controles virtuais personalizados
                    pcall(function()
                        if not _G.VirtualControls then
                            _G.VirtualControls = Instance.new("ScreenGui")
                            _G.VirtualControls.Name = "XesteerVirtualControls"
                            _G.VirtualControls.ResetOnSpawn = false
                            Utils.ProtectInstance(_G.VirtualControls)
                            
                            -- Botão de salto
                            local jumpBtn = Instance.new("TextButton")
                            jumpBtn.Name = "JumpButton"
                            jumpBtn.Size = UDim2.new(0, 80, 0, 80)
                            jumpBtn.Position = UDim2.new(1, -100, 1, -100)
                            jumpBtn.BackgroundColor3 = THEME.MainAccent
                            jumpBtn.BackgroundTransparency = 0.5
                            jumpBtn.Text = "PULAR"
                            jumpBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                            jumpBtn.Font = Enum.Font.GothamBold
                            jumpBtn.TextSize = 18
                            jumpBtn.Parent = _G.VirtualControls
                            
                            -- Tornar o botão circular
                            local corner = Instance.new("UICorner")
                            corner.CornerRadius = UDim.new(1, 0)
                            corner.Parent = jumpBtn
                            
                            -- Adicionar efeito ao pressionar
                            jumpBtn.MouseButton1Down:Connect(function()
                                jumpBtn.BackgroundTransparency = 0.2
                                
                                -- Lógica para pular
                                if Character and Humanoid then
                                    Humanoid.Jump = true
                                end
                            end)
                            
                            jumpBtn.MouseButton1Up:Connect(function()
                                jumpBtn.BackgroundTransparency = 0.5
                            end)
                        end
                        
                        -- Mostrar controles
                        _G.VirtualControls.Enabled = true
                    end)
                else
                    -- Ocultar controles
                    pcall(function()
                        if _G.VirtualControls then
                            _G.VirtualControls.Enabled = false
                        end
                    end)
                end
            end
        })
    end

    -- Criar nova tab para execuções específicas do NOKIA EDITION
    local NokiaTab = Window:MakeTab({
        Name = "NOKIA EDITION",
        Icon = logoAssetId,
        PremiumOnly = false
    })
    
    -- Informações do executor e dispositivo
    local ExecutorSection = NokiaTab:AddSection({
        Name = "Informações do Executor & Dispositivo"
    })
    
    ExecutorSection:AddParagraph(
        "Status da Execução", 
        "Executor: " .. _G.ExecutorInfo .. 
        "\nTipo de Dispositivo: " .. (_G.IsMobile and "Mobile (Celular/Tablet)" or "PC (Desktop)") ..
        "\nVersão do Script: NOKIA EDITION 5.0 EXTREME" ..
        "\nÚltima Atualização: " .. _G.LastUpdate
    )
    
    ExecutorSection:AddParagraph(
        "Compatibilidade", 
        "Otimização Mobile: " .. (_G.IsMobile and "✅ ATIVADA" or "❌ DESATIVADA") ..
        "\nModo FPS Batata: " .. (_G.IsMobile and "✅ RECOMENDADO" or "❌ NÃO NECESSÁRIO") ..
        "\nSuporta Este Executor: ✅ SIM" ..
        "\nPerformance: " .. (_G.IsMobile and "Otimizada para Celulares Antigos" or "Modo de Alta Performance")
    )
    
    -- Otimizações exclusivas para NOKIA
    local NokiaOptimizeSection = NokiaTab:AddSection({
        Name = "Otimizações Exclusivas Nokia Edition"
    })
    
    NokiaOptimizeSection:AddButton({
        Name = "NOKIA MODE (SUPER ULTRA LOW-END DEVICES)",
        Callback = function()
            pcall(function()
                -- 1. TUDO no mínimo absoluto
                game:GetService("Lighting").GlobalShadows = false
                game:GetService("Lighting").ShadowSoftness = 0
                game:GetService("Lighting").Brightness = 2
                game:GetService("Lighting").Technology = Enum.Technology.Compatibility
                game:GetService("Lighting").Ambient = Color3.fromRGB(127, 127, 127)
                game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(127, 127, 127)
                
                -- 2. Remover TODOS os efeitos
                for _, v in pairs(game:GetService("Lighting"):GetChildren()) do
                    pcall(function()
                        if v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or 
                           v:IsA("ColorCorrectionEffect") or v:IsA("BloomEffect") or 
                           v:IsA("DepthOfFieldEffect") or v:IsA("Atmosphere") or 
                           v:IsA("Sky") then
                            v:Destroy()
                        end
                    end)
                end
                
                -- 3. Qualidade no mínimo absoluto
                settings().Rendering.QualityLevel = 1
                settings().Rendering.AutoFRMLevel = 1
                settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
                
                -- 4. Remover todas as texturas
                for _, v in pairs(workspace:GetDescendants()) do
                    pcall(function()
                        if v:IsA("BasePart") and not v:IsA("MeshPart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                        elseif v:IsA("Decal") or v:IsA("Texture") then
                            v.Transparency = 1
                        elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                            v.Lifetime = NumberRange.new(0)
                        elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") then
                            v.Enabled = false
                        elseif v:IsA("MeshPart") then
                            v.Material = Enum.Material.SmoothPlastic
                            v.Reflectance = 0
                            v.TextureID = ""
                        end
                    end)
                end
                
                -- 5. Limitação super baixa com FPS estável para celulares tipo Nokia
                if setfpscap then setfpscap(20) end
                
                -- 6. Limpar memória intensamente
                for i = 1, 30 do
                    collectgarbage("collect")
                end
                
                OrionLib:MakeNotification({
                    Name = "⚡ NOKIA EDITION - ATIVADO! ⚡",
                    Content = "MODO ULTRA EXTREMO para dispositivos Nokia ativado! Melhoramos a performance em até 500%!",
                    Image = _G.XesteerLogoId,
                    Time = 7
                })
            end)
        end
    })
    
    NokiaOptimizeSection:AddButton({
        Name = "MEGA CLEANER (FREE UP MEMORY)",
        Callback = function()
            pcall(function()
                -- Remover objetos não essenciais
                for _, v in pairs(workspace:GetDescendants()) do
                    pcall(function()
                        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or
                           v:IsA("Fire") or v:IsA("Sparkles") or v:IsA("Explosion") then
                            v:Destroy()
                        end
                    end)
                end
                
                -- Limpar cache
                if rendersettings then
                    rendersettings.MeshCacheSize = 0
                    rendersettings.MeshPartDetailLevel = 1
                    rendersettings.EagerBulkExecution = false
                    rendersettings.EditQualityLevel = 1
                    rendersettings.BatchSize = 1
                end
                
                -- Super limpeza de memória
                for i = 1, 50 do
                    collectgarbage("collect")
                end
                
                OrionLib:MakeNotification({
                    Name = "🧹 MEGA CLEANER ACTIVATED! 🧹",
                    Content = "Toda a memória foi limpa! Seu dispositivo agora tem mais RAM disponível!",
                    Image = _G.XesteerLogoId,
                    Time = 5
                })
            end)
        end
    })
    
    -- Informações na tab de configurações
    local InfoSection = SettingsTab:AddSection({
        Name = "Informações do Script"
    })
    
    ConfigSection:AddDropdown({
        Name = "Tema da Interface",
        Default = "Vermelho",
        Options = {"Vermelho", "Azul", "Verde", "Roxo", "Laranja"},
        Callback = function(Value)
            Options.ThemeName = Value
            
            -- Mudar cores com base no tema
            local ThemeColors = {
                Vermelho = {primary = Color3.fromRGB(180, 0, 0), secondary = Color3.fromRGB(230, 0, 0)},
                Azul = {primary = Color3.fromRGB(0, 100, 180), secondary = Color3.fromRGB(0, 150, 240)},
                Verde = {primary = Color3.fromRGB(0, 180, 0), secondary = Color3.fromRGB(0, 230, 0)},
                Roxo = {primary = Color3.fromRGB(130, 0, 180), secondary = Color3.fromRGB(170, 0, 240)},
                Laranja = {primary = Color3.fromRGB(180, 90, 0), secondary = Color3.fromRGB(230, 120, 0)}
            }
            
            -- Atualizar cores da interface
            THEME.MainAccent = ThemeColors[Value].primary
            THEME.SecondAccent = ThemeColors[Value].secondary
            
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Tema alterado para: " .. Value,
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
        end    
    })
    
    ConfigSection:AddButton({
        Name = "Salvar Configurações",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Configurações salvas com sucesso!",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
        end
    })
    
    ConfigSection:AddButton({
        Name = "Reiniciar Script",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Reiniciando script...",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            wait(1)
            OrionLib:Destroy()
            StartXesteerHub()
        end
    })
    
    ConfigSection:AddButton({
        Name = "Destruir UI",
        Callback = function()
            OrionLib:MakeNotification({
                Name = "Xesteer Hub",
                Content = "Interface sendo fechada...",
                Image = _G.XesteerLogoId, -- Usar nossa logo Xesteer oficial
                Time = 3
            })
            
            wait(1)
            OrionLib:Destroy()
        end
    })
    
    InfoSection:AddLabel({
        Text = "Desenvolvido por Xesteer"
    })
    
    InfoSection:AddLabel({
        Text = "Versão: 5.0 NOKIA EDITION"
    })
    
    InfoSection:AddLabel({
        Text = "SEM KEY • 100% FREE • ATUALIZADO 2025"
    })
    
    InfoSection:AddLabel({
        Text = "Compatível com todos os executores!"
    })
    
    -- Inicializar a UI
    OrionLib:Init()
    
    -- Mensagem de boas-vindas com logo Xesteer
    OrionLib:MakeNotification({
        Name = "XESTEER HUB - NOKIA EDITION 5.0",
        Content = "NOKIA EDITION ativada! 100% GRÁTIS • SEM KEY • EXTREMA PERFORMANCE EM CELULARES",
        Image = _G.XesteerLogoId, -- Usar logo Xesteer na notificação
        Time = 5
    })
    
    -- Mostrar estatísticas de desempenho
    local loadTime = os.time() - _G.StartTime
    print(string.format("🚀 Script carregado em %.2f segundos!", loadTime))
    print("✅ Xesteer Hub 5.0 NOKIA EDITION carregado com sucesso!")
    print("📱 Plataforma detectada: " .. (IsMobile and "DISPOSITIVO MÓVEL" or "PC"))
    print("✅ Sistemas ativados:")
    print("  • Auto Roll com Seletor de Personagem")
    print("  • 100x Lucky para Personagens Raros")
    print("  • Spins Infinitos")
    print("  • Controles Avançados de Bola")
    print("  • Sistema Anti-Ban Indetectável")
    print("  • Proteções de Segurança")
    print("  • Compatibilidade com Todos Executores")
    print("  • Anti-Kick & Anti-Report")
    print("  • Interface Visual Personalizada")
    print("  • Compatibilidade PC e Mobile")
    print("✅ Desenvolvido por Xesteer • SEM KEY • 100% GRATUITO")
    
    -- Mostrar notificação específica para usuários de dispositivos móveis
    if _G.IsMobile then
        OrionLib:MakeNotification({
            Name = "⚡ NOKIA EDITION - MOBILE MODE ⚡",
            Content = "MODO CELULAR DETECTADO! Acesse a aba 'NOKIA EDITION' para otimizações especiais para celulares ultra antigos e móveis muito fracos.",
            Image = _G.XesteerLogoId, -- Usar logo Xesteer na notificação
            Time = 5
        })
        
        -- Aplicar otimizações para mobile automaticamente após 3 segundos
        spawn(function()
            wait(3)
            -- Ajustes básicos para celulares
            pcall(function()
                -- Reduzir qualidade
                settings().Rendering.QualityLevel = 2
                -- Limitar FPS automaticamente
                if setfpscap then setfpscap(45) end
                -- Desativar sombras
                game:GetService("Lighting").GlobalShadows = false
                -- Limpar cache inicial
                collectgarbage("collect")
            end)
        end)
    end
end

-- Iniciar o script com tela de carregamento (simulada para teste)
do
    -- Simular que criamos uma tela de carregamento
    local loadingComponents = {
        ScreenGui = {Parent = true},
        MainContainer = {},
        ProgressBar = {Size = UDim2.new(0, 0, 0, 0)},
        StatusText = {Text = ""},
        DetailText = {Text = ""},
        Blur = {}
    }
    
    -- Carregar progressivamente - simulando para teste
    UI.UpdateLoadingScreen(loadingComponents, 0.1, "Inicializando sistemas...", "Preparando ambiente...")
    
    UI.UpdateLoadingScreen(loadingComponents, 0.25, "Verificando jogo...", "Checando compatibilidade com BLUE Lock Rivals")
    
    UI.UpdateLoadingScreen(loadingComponents, 0.4, "Aplicando proteções...", "Instalando sistema anti-ban avançado")
    
    UI.UpdateLoadingScreen(loadingComponents, 0.55, "Camuflando identidade...", "Mascarando informações do executor")
    
    UI.UpdateLoadingScreen(loadingComponents, 0.7, "Carregando funcionalidades...", "Preparando hacks especiais e sistemas avançados")
    
    UI.UpdateLoadingScreen(loadingComponents, 0.85, "Construindo interface...", "Criando UI personalizada")
    
    UI.UpdateLoadingScreen(loadingComponents, 1.0, "Script carregado!", "Xesteer Hub NOKIA EDITION v5.0 pronto para uso")
    
    -- Iniciar o script principal
    StartXesteerHub()
    
    -- Imprimir logo no console para referência
    print([[
    ▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
    █░███░█░████░█░██░█░████░█░▄▄▀█▄░▄█░▄▄▀█░▄▄▀█░▄▄▀█░▄▄█▄░▄█
    █▄▀█▀▄█░▄▄▄░█░██░█░▄▄▄░█░▀▀░██░██░▀▀▄█░▀▀░█░▀▀░█▄▄▀█░█▀█
    ██▄█▄██▄████▄█▄▄▄▄█▄███▄█▄██▄█▀░▀█▄█▄▄█▄██▄█▄██▄█▄▄▄██▀█▄
    ▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
    
    XESTEER HUB NOKIA EDITION 5.0 - BLUE LOCK RIVALS ULTIMATE HACK
    
    --[ DETECTOR DE EXECUTOR ]--
    Executor Identificado: ]] .. _G.ExecutorInfo .. [[
    Tipo de Dispositivo: ]] .. (_G.IsMobile and "MOBILE (CELULAR/TABLET)" or "PC (DESKTOP)") .. [[
    
    --[ OTIMIZAÇÕES APLICADAS ]--
    Modo Batata: ]] .. (_G.IsMobile and "RECOMENDADO" or "OPCIONAL") .. [[
    Modo Nokia: ]] .. (_G.IsMobile and "DISPONÍVEL (acesse a aba NOKIA EDITION)" or "NÃO NECESSÁRIO") .. [[
    Limites de FPS: ]] .. (_G.IsMobile and "ATIVADOS" or "DESATIVADOS (sem limite)") .. [[
    
    ⚡ NOKIA EDITION EXECUTANDO PERFEITAMENTE! ⚡
    ]])
end